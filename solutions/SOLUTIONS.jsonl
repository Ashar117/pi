{"id": "S-006", "ticket_ids": ["T-006", "T-007"], "problem": "self.messages cleared on mode switch destroyed session context and caused session summary to silently skip on exit", "countermeasure": "Removed all 'self.messages = []' from mode switch handlers in process_input(). Changed exit summary guard from 'if self.messages:' to 'if self.messages or self.history:'. Added self.history fallback in _generate_session_summary() so summary is built from string history when messages list is empty.", "files_changed": ["pi_agent.py"], "result": "worked", "better_future_fix": "Consider making self.messages and self.history a unified session store. The current dual-list design (messages=API format, history=string format) is a source of bugs when one is empty and the other is not.", "lessons": ["Never wipe session state on mode switches â€” mode is a routing decision, not a session boundary", "Exit guards that depend on mutable state must account for all paths that could empty that state", "When two data structures track the same thing in different formats, they will diverge"], "date": "2026-04-21T03:30:00Z", "recurring": false}
{"id": "S-007", "ticket_ids": ["T-008", "T-009"], "problem": "L1 tier not implemented in memory_write/read. Mode switch commands required exact string match.", "countermeasure": "Added elif tier='l1' branch in memory_write() writing to raw_wiki Supabase table with correct schema. Added L1 read support in memory_read(). Changed mode switch string equality to set membership check supporting natural variants.", "files_changed": ["tools/tools_memory.py", "pi_agent.py"], "result": "worked", "better_future_fix": "L1 write should auto-assign thread_id from a session-level UUID, not generate a new one per write. Mode switching should eventually support fuzzy matching or be handled by the LLM itself.", "lessons": ["Architecture docs and SQL schemas mean nothing if the code doesn't implement them â€” always trace the full path from spec to implementation", "Exact string matching for user commands breaks the moment users type naturally"], "date": "2026-04-21T03:30:00Z", "recurring": false}
{"id": "S-008", "ticket_ids": ["T-010", "T-011", "T-014"], "problem": "Memory writes were functionally invisible: (1) get_l3_context() hardcoded 5 category keys, silently dropping session_history, research_results, file_operations, note â€” Pi had no continuity between sessions despite writing summaries. (2) _sync_l3() called on every message â€” full Supabase fetch + SQLite wipe per prompt. (3) _verify_write() only checked SQLite, so silent Supabase failures returned verified=True â€” entries disappeared on next restart.", "countermeasure": "T-010: Replaced hardcoded sections dict with dynamic category grouping in get_l3_context() â€” all categories now appear in context output. Added priority_order list and label_map for clean formatting. T-011: Added _last_sync timestamp and _sync_ttl_seconds=300 to MemoryTools. get_l3_context() now only calls _sync_l3() when cache is stale (>5min). _sync_l3() updates _last_sync on success. T-014: _verify_write() now queries both SQLite AND Supabase l3_active_memory â€” verified=True only if both contain the entry.", "files_changed": ["tools/tools_memory.py"], "result": "worked", "better_future_fix": "get_l3_context() 800-token budget is shared across all categories. Session history (importance=4) competes with profile (importance=10) and frequently gets truncated. Consider a per-category token budget. Also consider async Supabase writes so memory_write() doesn't block the response.", "lessons": ["A memory system that writes but doesn't inject is worse than no memory â€” it creates false confidence", "Any hardcoded category list will eventually diverge from what the code actually writes", "verified=True should mean 'will survive a restart' not 'was written to cache'"], "date": "2026-04-22T01:00:00Z", "recurring": false}
{"id": "S-009", "ticket_ids": ["T-012", "T-013"], "problem": "(1) self.messages[-20:] truncation could orphan a tool_result block from its tool_use pair â€” latent API 400 crash after any long multi-tool session. (2) No session_id anywhere in the system â€” evolution logs, L1 raw_wiki writes, and tool traces couldn't be correlated to a single session.", "countermeasure": "T-012: Replaced naive slice with safe truncation loop â€” walks forward from the slice point until it lands on a plain user text message, never cutting inside a tool_use/tool_result pair. T-013: Added self.session_id = uuid4().hex[:8] to PiAgent.__init__(), printed on startup. session_id injected into evolution log metadata for both root and normie modes. session_id passed as optional arg to memory_write() and used as thread_id for L1 raw_wiki entries so all L1 writes in a session share one thread.", "files_changed": ["pi_agent.py", "tools/tools_memory.py"], "result": "worked", "better_future_fix": "session_id should also be written into the session summary itself (L3 session_history entry) so you can grep evolution.jsonl by session_id and find the corresponding summary in Supabase. Evolution log entries should have a top-level session_id field, not buried in metadata.", "lessons": ["Paired message structures (tool_use / tool_result) must never be split by truncation â€” always find a safe boundary", "Without session correlation IDs, logs are a pile of disconnected events, not a history"],"date": "2026-04-22T01:00:00Z", "recurring": false}
{"id": "S-010", "ticket_ids": ["T-015"], "problem": "Mode-switch handler in process_input() used strict tuple-membership equality. Natural-language requests like 'can u switch to root mode ?' did not match. The agent stayed in normie mode while the LLM, prompted into a switch context by the user, hallucinated the entire mode-switch experience (fake banner, fake 'type confirm' flow). Worse: the user believed they were in root mode for the rest of the session, so every Pi claim of tool use during that period was fabricated.", "countermeasure": "Replaced strict matcher with a loose detector: short messages (<= 8 words after stripping punctuation) containing 'root mode' or 'normie mode' trigger the switch. analyze performance / research mode / exit remain strict. Added re import. Switch to using cmd_clean (punctuation-stripped) consistently in the strict branches as well.", "files_changed": ["pi_agent.py"], "result": "worked", "better_future_fix": "Eventually have the LLM itself emit a structured mode_switch tool call, so the agent never has to parse natural language for mode intent. Until then, consider an explicit /root and /normie slash-command convention that's both terse and unambiguous.", "lessons": ["Strict command parsing is hostile to natural conversation â€” users phrase the same intent ten different ways", "When the agent fails to recognise an intent, the LLM tries to satisfy the user anyway by miming the missing capability â€” silent failure mode worse than a refusal"], "date": "2026-04-24T22:15:00Z", "recurring": false}
{"id": "S-011", "ticket_ids": ["T-016"], "problem": "_respond_normie() appended only to self.history (a string-only research-mode helper), never to self.messages (the unified Claude-API message store). After mode switched normie -> root, Claude received an empty (or stale) message list. The session looked brand new to Claude, even though the user had had a long conversation seconds earlier. Continuity lost. The L-001 invariant ('mode is not a session boundary') was indirectly violated.", "countermeasure": "(1) _respond_normie now appends both user input and assistant response to self.messages. session_ctx is built from self.messages BEFORE the new user turn is appended, so it is not double-counted. (2) Lifted the safe-truncation logic from _respond_root into a shared _truncate_messages_safely() helper and call it from both modes after each turn.", "files_changed": ["pi_agent.py"], "result": "worked", "better_future_fix": "Eliminate self.history entirely. Have research mode pull its string-only context directly from self.messages via _extract_text_from_messages(). One source of truth, no drift possible.", "lessons": ["Two parallel state stores will always drift â€” drop one or pay the cost forever", "Mode boundaries hide bugs because each mode looks correct in isolation; cross-mode behavior is where invariants break"], "date": "2026-04-24T22:20:00Z", "recurring": false}
{"id": "S-012", "ticket_ids": ["T-020"], "problem": "evolution.py write/read schema drift (SM-001): log_interaction wrote field 'tools_used' (list of name strings), analyze_performance read field 'tool_calls' (list of {id,name,input} dicts). The two field names never agreed, so tool_usage and tool_success_rates returned {} on every call since the analyzer was written. Real production logs (107 interactions across 4 sessions) silently passed through identify_improvements with empty tool data â€” every monthly-self-review tool-failure branch was unreachable.", "countermeasure": "Two edits to evolution.py. (1) log_interaction now writes both 'tools_used' (kept for at-a-glance display) and 'tool_calls' (the structured list, what the analyzer wants). It also extracts session_id from metadata and writes it at the top level of the entry, so per-session log queries do not have to dig into metadata. (2) analyze_performance reads 'tool_calls' first; if absent, materializes [{'name': n} for n in tools_used] from the legacy field â€” every entry already on disk (107 interactions) stays analyzable through the upgrade. Added a per-session breakdown (sessions dict: interactions/successful/failed/cost keyed by session_id) read with the same top-level-first / metadata-fallback pattern.", "files_changed": ["evolution.py", "testing/test_evolution_schema.py"], "result": "worked", "better_future_fix": "The drift was only catchable because someone went looking. A schema test should run as part of run_all_tests.py â€” wired in via Phase 6 verify.py. Also: the entire analyze_performance function does no try/except per-line on json.loads, so one malformed entry crashes the whole report. get_recent_interactions does, inconsistently. Worth unifying.", "lessons": ["Telemetry schemas drift silently because nothing crashes â€” the analyzer just returns empty dicts forever. Test the round trip from log_interaction to analyze_performance with an asserted populated result.", "Backward compatibility on log files matters: a fix that breaks the analyzability of every prior entry is worse than the original drift. Always provide a fallback read path for legacy fields when adding new ones.", "session_id at top level is queryable; session_id buried in metadata is invisible to grep/jq one-liners. If a field is going to be used for grouping, surface it."], "date": "2026-04-25T00:50:00Z", "recurring": false}
{"id": "S-013", "ticket_ids": ["T-017", "T-021"], "problem": "Two related drift bugs in tools/tools_memory.py memory_read. (T-017) Docstring promised tier=None searches all tiers but code at line 96 gated L1 on `if tier == \"l1\":` \u2014 exclusive. Pi reasons about its capabilities from the docstring; the lie produced wrong self-descriptions. (T-021/SM-003) L2 content search filtered on title (first 100 chars of content) only. Distinctive keywords past char 100 were unreachable via memory_read(tier='l2'). Reproduced both: T-017 via testing/test_memory_tier_contract.py; T-021 via testing/test_l2_content_search.py \u2014 wrote an L2 entry with the marker in the body past char 100, searched, got 0 results pre-fix.", "countermeasure": "T-017: rewrote memory_read docstring to match the code \u2014 tier=None searches L3+L2 only, L1 is opt-in via tier='l1' explicitly, with a one-line note about the deferred aggressive fix. Zero code-behaviour change. T-021: in the L2 branch of memory_read, do two queries when query is non-empty \u2014 one ilike on title, one ilike on content->>text (PostgREST JSON-path operator, confirmed working on supabase-py 2.28.3 via direct probe before applying). Merge results by id, dedupe, slice to limit. Empty-query behaviour unchanged.", "files_changed": ["tools/tools_memory.py", "testing/test_memory_tier_contract.py", "testing/test_l2_content_search.py", "testing/test_memory_roundtrip.py", "testing/test_session_id_propagation.py"], "result": "worked", "better_future_fix": "L1 search currently does ilike on raw_wiki.content with no rate limit and no full-text index. The aggressive T-017 fix (include L1 in tier=None) is deferred until raw_wiki has a tsvector index. Adding the index would also speed up L1 explicit searches for users who do pass tier='l1'. L2 content search now does two Supabase round-trips per query; could be folded into one with a PostgREST or_ filter (`title.ilike.*query*,content->>text.ilike.*query*`) once the syntax is locked-down across supabase-py versions.", "lessons": ["A docstring is a contract that something (a future caller, a future reader, or in Pi's case the consciousness prompt) reads and trusts. Lying docstrings cause real misbehaviour even when the code is correct.", "Title-only search with body content under JSONB is the same shape of drift as evolution.tools_used vs tool_calls (S-012/L-011) \u2014 write path stores in one place, read path looks in another. Same class of bug, different layer.", "When a JSON-column filter syntax depends on the exact client-library version, probe it against real data before designing the fix. supabase-py 2.28.3 supports both `.ilike(\"content->>text\", ...)` and `.filter(\"content->>text\", \"ilike\", ...)`; older versions might not."], "date": "2026-04-26T01:00:00Z", "recurring": false}
{"id": "S-014", "ticket_ids": ["T-019"], "problem": "Three prompt-side bugs. (1) C-009: consciousness.txt listed web_search as a tool under 'When to Search vs Remember', in auto-proceed permissions, and in tool-chaining examples â€” the tool does not exist. Every session where Pi tried to satisfy a web-search request was silently lying. (2) C-008: consciousness.txt had no memory_read query-formation rules â€” Claude issued narrative paraphrase queries ('Ash subway order food preferences') instead of short keyword queries ('subway'), producing 0-result searches for content that was clearly in memory. (3) T-019: normie mode refusal was partially covered by the 'Never Mime Tool Use' section but lacked a per-case refusal table, leaving Groq to improvise and sometimes produce persistence claims.", "countermeasure": "Three edits to prompts/consciousness.txt. (1) Replaced the ghost web_search references in 'When to Search vs Remember', auto-proceed, and tool-chain examples with honest 'no web_search tool exists' statements. Removed 'Searching web' from auto-proceed. (2) Added MEMORY_READ QUERY FORMATION RULES section: use 1-3 keywords from user message; first query short and literal; retry with single keyword on 0 results; try tier='l2' after 2 failures; never narrate the search; never invent an answer after 2 failed queries. (3) Added NORMIE MODE REFUSAL TABLE to the Never-Mime-Tool-Use section with exact response phrases for: 'remember X', 'what did I tell you about Y', 'run code', 'search for X', 'switch to root'. Updated tool list to include create_file (was missing) and added 'No web_search, no email, no calendar' footer. Complete rewrite of prompts/system.txt: 19 vague lines â†’ structured normie-context document with identity, mode explanation, full tool suite list (for Groq's awareness), normie refusal table, and hard rules.", "files_changed": ["prompts/consciousness.txt", "prompts/system.txt", "testing/test_normie_honesty.py", "testing/test_query_formulation.py", "testing/test_mode_switch_natural.py"], "result": "worked", "better_future_fix": "test_query_formulation.py's keyword-length assertion (â‰¤4 tokens) may need loosening if Claude legitimately uses 5-token queries that still contain the right keyword. Consider relaxing to 'must contain a stored keyword' as the primary assertion and 'must be â‰¤6 tokens' as a secondary. test_normie_honesty.py requires a live Groq call â€” if Groq is down or rate-limited, the test fails in CI for reasons unrelated to the prompt. Add a GROQ_AVAILABLE skip guard.", "lessons": ["Ghost tool references in the prompt are worse than silence â€” they actively steer Claude toward a capability that doesn't exist and produce confident-sounding lies rather than honest refusals.", "A tool description that says 'call memory_read when you need memory' but says nothing about how to formulate the query produces long narrative queries that miss stored content. The how matters as much as the when.", "A refusal table with exact phrases is more reliable than a general prohibition â€” LLMs under pressure will find the edge of any abstract rule, but specific forbidden/allowed phrase pairs leave no ambiguity."], "date": "2026-05-02T00:00:00Z", "recurring": false}
{"id": "S-015", "ticket_ids": ["T-022"], "problem": "Multiple Python scripts in the testing/ directory printed Unicode glyphs (✓ and ✗) inline with f-strings. On Windows Python 3.13 with default cp1252 stdout, `python testing/test_requirements.py` raised UnicodeEncodeError: 'charmap' codec can't encode character. The env-var checking logic itself worked; only the printout crashed. Same class of bug latent in test_runner.py, run_all_tests.py, test_modes.py, test_persistence.py, test_integration.py. pi_agent.py also printed box-drawing chars (U+2550) and check marks via agent/health.py, but those run only through the pi_agent.py entry point.", "countermeasure": "Two-track fix per T-022 recommendation. (a) Replaced every ✓ with [OK] and every ✗ with [FAIL] across 6 testing/ scripts: test_modes.py, test_integration.py, test_persistence.py, test_requirements.py, test_runner.py, run_all_tests.py. Verified by grep [✓✗] across testing/ → 0 matches. (b) Confirmed pi_agent.py already had `if hasattr(sys.stdout, 'reconfigure'): sys.stdout.reconfigure(encoding='utf-8', errors='replace')` at lines 10-13 (added in earlier Phase 4-6 work, not in this fix). agent/health.py has no __main__ block, so it inherits pi_agent.py's reconfigure when invoked. agent/prompt.py's box-drawing chars in the mode block are emitted only into the system prompt (string), never to stdout, so they're safe regardless.", "files_changed": ["testing/test_modes.py", "testing/test_integration.py", "testing/test_persistence.py", "testing/test_requirements.py", "testing/test_runner.py", "testing/run_all_tests.py"], "result": "worked", "better_future_fix": "Add a CI step that runs each testing/*.py script under PYTHONIOENCODING=cp1252 to catch this class of bug on first commit instead of months later. Could fold into Phase 6's verify.py: `for f in testing/*.py: PYTHONIOENCODING=cp1252 python -c 'import ast; ast.parse(open(f).read())' or just shell out and assert returncode==0`. Also: the choice of [OK]/[FAIL] is plain ASCII but loses some visual contrast; a follow-up could use `ANSI green/red` color codes via a small print helper, falling back to plain on dumb terminals.", "lessons": ["Pretty-printed Unicode (check marks, box drawing, em-dashes) is a portability liability on Windows. Pick ASCII-safe sigils for diagnostic output unless you control the terminal encoding at the entry point.", "A `sys.stdout.reconfigure(encoding='utf-8')` block at the top of every entry point is the easy escape hatch for the box-drawing aesthetic. Use it intentionally; do not assume it propagates to scripts that get launched independently."], "date": "2026-05-02T00:00:00Z", "recurring": false}
{"id": "S-016", "ticket_ids": ["T-023"], "problem": "The Phase 3 round-trip canary (test_memory_roundtrip.py) returned VERDICT: GREEN but agent #2 made zero tool calls. The marker had been synced from Supabase to SQLite by _sync_l3(), surfaced in get_l3_context(), and Claude read it directly from the system prompt. The memory_read tool path \u2014 the path that breaks in production (LOG1/LOG2) \u2014 was never exercised. The test verified storage and L3 ambient injection; it said nothing about whether Claude formulates good memory_read queries.", "countermeasure": "Created testing/test_memory_tool_path.py with three pytest tests. (1) test_memory_read_tool_fires: writes marker to L2 only (organized_memory, which is never loaded into get_l3_context()), creates fresh agent, monkey-patches _execute_tool to capture memory_read calls, asserts captured_queries is non-empty. (2) test_recall_response_contains_marker: asserts l2_marker.lower() in response.lower(). (3) test_query_contains_useful_keyword: asserts the captured query contains \"zx9\", \"codeword\", or \"secret\" \u2014 catching the paraphrase-query failure mode from LOG1/LOG2. Writing to L2 is the key forcing function: L2 is never auto-loaded into L3 context, so Claude must call memory_read or admit it does not know. Marked @pytest.mark.costly (~/usr/bin/bash.002/run). Companion fix S-013 (T-021) had already fixed the L2 content-search filter to search content->>text, not just title \u2014 so the memory_read call path now actually returns the entry.", "files_changed": ["testing/test_memory_tool_path.py"], "result": "worked", "better_future_fix": "The three tests each create a fresh PiAgent, which means 3 Supabase syncs and 3 Claude API calls per run. Could be refactored to share the l2_marker fixture and reuse a single agent across tests via module-scoped agent fixture, or split into separate costly/free markers. Also: test_query_contains_useful_keyword skips rather than fails when no memory_read fired \u2014 this means test ordering matters. Consider asserting the tool call first in a shared fixture step.", "lessons": ["A round-trip test satisfied by ambient context proves nothing about the tool call path. Force the tool path by writing to a tier that cannot be ambiently loaded (L2 for Pi).", "When the test fixture writes a unique enough marker, a query formulation failure is observable (0 results) without needing to inspect Claude text output directly \u2014 check captured query strings."], "date": "2026-05-03T00:00:00Z", "recurring": false}
{"id": "S-017", "ticket_ids": ["T-024"], "problem": "L1 (raw_wiki) was populated only by explicit Claude tool calls to memory_write(tier=\"l1\"), which are rare and not guaranteed. Per-turn conversation capture never happened automatically. Two latent bugs compounded this: (1) memory_write(tier=\"l1\") derived thread_id directly from session_id (8 hex chars), but raw_wiki.thread_id is UUID NOT NULL \u2014 every explicit L1 write silently failed Supabase validation. (2) Without auto-logging, Phase B autonomy (Pi reading its own interaction archive) had no data to read.", "countermeasure": "Three changes. (1) Added MemoryTools.log_turn() -- batch-inserts user row + tool rows (one per call, in order) + assistant row to raw_wiki in a single Supabase insert. Uses a proper UUID thread_id passed by the caller. Best-effort: exceptions print a non-fatal warning and never propagate. (2) Fixed memory_write(tier='l1') thread derivation: replaced session_id-as-thread_id with uuid5(NAMESPACE_DNS, session_id). uuid5 is deterministic per session_id, gives a valid UUID, and produces the same thread_id as log_turn() for the same session -- so tool-path and auto-log rows share thread_id (T-013). session_id is now also stored in L1 metadata for cross-log correlation. (3) In pi_agent.py __init__: added self.l1_thread_id = str(uuid.uuid5(uuid.NAMESPACE_DNS, self.session_id)) and self.turn_number = 0. In _respond_root: added l1_tool_records list populated inside the tool loop with {name, input, result_summary[:500]}, then log_turn() called after evolution.log_interaction. In _respond_normie: log_turn() called after evolution.log_interaction (no tool calls). Also moved import uuid from inside method bodies to top-level in tools_memory.py.", "files_changed": ["tools/tools_memory.py", "pi_agent.py", "testing/test_l1_autolog.py"], "result": "worked", "better_future_fix": "log_turn() fires synchronously, adding ~1 Supabase round-trip per turn. For very fast sessions this is acceptable; for interactive low-latency use, consider fire-and-forget threading (threading.Thread(target=self.memory.log_turn, ...).start()). Also: raw_wiki.timestamp is the same for all rows in a turn (snapshot of turn start time). Turn ordering within a session is stable via turn_number in metadata, but if two turns fire close together, clock-based ordering of threads could interleave. An explicit sequence field on raw_wiki would be cleaner long-term.", "lessons": ["A UUID column that receives a short hex string fails silently at the Supabase constraint level \u2014 the Python client catches the error, logs it, and returns success=False with no visible crash. Always verify the types of all fields against the Supabase schema before writing.", "When two code paths should produce the same UUID for the same logical key (session), use a deterministic derivation (uuid5) rather than storing the UUID separately in both paths \u2014 divergence is guaranteed if the storage drifts."], "date": "2026-05-03T00:00:00Z", "recurring": false}

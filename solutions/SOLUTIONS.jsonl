{"id": "S-006", "ticket_ids": ["T-006", "T-007"], "problem": "self.messages cleared on mode switch destroyed session context and caused session summary to silently skip on exit", "countermeasure": "Removed all 'self.messages = []' from mode switch handlers in process_input(). Changed exit summary guard from 'if self.messages:' to 'if self.messages or self.history:'. Added self.history fallback in _generate_session_summary() so summary is built from string history when messages list is empty.", "files_changed": ["pi_agent.py"], "result": "worked", "better_future_fix": "Consider making self.messages and self.history a unified session store. The current dual-list design (messages=API format, history=string format) is a source of bugs when one is empty and the other is not.", "lessons": ["Never wipe session state on mode switches â€” mode is a routing decision, not a session boundary", "Exit guards that depend on mutable state must account for all paths that could empty that state", "When two data structures track the same thing in different formats, they will diverge"], "date": "2026-04-21T03:30:00Z", "recurring": false}
{"id": "S-007", "ticket_ids": ["T-008", "T-009"], "problem": "L1 tier not implemented in memory_write/read. Mode switch commands required exact string match.", "countermeasure": "Added elif tier='l1' branch in memory_write() writing to raw_wiki Supabase table with correct schema. Added L1 read support in memory_read(). Changed mode switch string equality to set membership check supporting natural variants.", "files_changed": ["tools/tools_memory.py", "pi_agent.py"], "result": "worked", "better_future_fix": "L1 write should auto-assign thread_id from a session-level UUID, not generate a new one per write. Mode switching should eventually support fuzzy matching or be handled by the LLM itself.", "lessons": ["Architecture docs and SQL schemas mean nothing if the code doesn't implement them â€” always trace the full path from spec to implementation", "Exact string matching for user commands breaks the moment users type naturally"], "date": "2026-04-21T03:30:00Z", "recurring": false}
{"id": "S-008", "ticket_ids": ["T-010", "T-011", "T-014"], "problem": "Memory writes were functionally invisible: (1) get_l3_context() hardcoded 5 category keys, silently dropping session_history, research_results, file_operations, note â€” Pi had no continuity between sessions despite writing summaries. (2) _sync_l3() called on every message â€” full Supabase fetch + SQLite wipe per prompt. (3) _verify_write() only checked SQLite, so silent Supabase failures returned verified=True â€” entries disappeared on next restart.", "countermeasure": "T-010: Replaced hardcoded sections dict with dynamic category grouping in get_l3_context() â€” all categories now appear in context output. Added priority_order list and label_map for clean formatting. T-011: Added _last_sync timestamp and _sync_ttl_seconds=300 to MemoryTools. get_l3_context() now only calls _sync_l3() when cache is stale (>5min). _sync_l3() updates _last_sync on success. T-014: _verify_write() now queries both SQLite AND Supabase l3_active_memory â€” verified=True only if both contain the entry.", "files_changed": ["tools/tools_memory.py"], "result": "worked", "better_future_fix": "get_l3_context() 800-token budget is shared across all categories. Session history (importance=4) competes with profile (importance=10) and frequently gets truncated. Consider a per-category token budget. Also consider async Supabase writes so memory_write() doesn't block the response.", "lessons": ["A memory system that writes but doesn't inject is worse than no memory â€” it creates false confidence", "Any hardcoded category list will eventually diverge from what the code actually writes", "verified=True should mean 'will survive a restart' not 'was written to cache'"], "date": "2026-04-22T01:00:00Z", "recurring": false}
{"id": "S-009", "ticket_ids": ["T-012", "T-013"], "problem": "(1) self.messages[-20:] truncation could orphan a tool_result block from its tool_use pair â€” latent API 400 crash after any long multi-tool session. (2) No session_id anywhere in the system â€” evolution logs, L1 raw_wiki writes, and tool traces couldn't be correlated to a single session.", "countermeasure": "T-012: Replaced naive slice with safe truncation loop â€” walks forward from the slice point until it lands on a plain user text message, never cutting inside a tool_use/tool_result pair. T-013: Added self.session_id = uuid4().hex[:8] to PiAgent.__init__(), printed on startup. session_id injected into evolution log metadata for both root and normie modes. session_id passed as optional arg to memory_write() and used as thread_id for L1 raw_wiki entries so all L1 writes in a session share one thread.", "files_changed": ["pi_agent.py", "tools/tools_memory.py"], "result": "worked", "better_future_fix": "session_id should also be written into the session summary itself (L3 session_history entry) so you can grep evolution.jsonl by session_id and find the corresponding summary in Supabase. Evolution log entries should have a top-level session_id field, not buried in metadata.", "lessons": ["Paired message structures (tool_use / tool_result) must never be split by truncation â€” always find a safe boundary", "Without session correlation IDs, logs are a pile of disconnected events, not a history"],"date": "2026-04-22T01:00:00Z", "recurring": false}
{"id": "S-010", "ticket_ids": ["T-015"], "problem": "Mode-switch handler in process_input() used strict tuple-membership equality. Natural-language requests like 'can u switch to root mode ?' did not match. The agent stayed in normie mode while the LLM, prompted into a switch context by the user, hallucinated the entire mode-switch experience (fake banner, fake 'type confirm' flow). Worse: the user believed they were in root mode for the rest of the session, so every Pi claim of tool use during that period was fabricated.", "countermeasure": "Replaced strict matcher with a loose detector: short messages (<= 8 words after stripping punctuation) containing 'root mode' or 'normie mode' trigger the switch. analyze performance / research mode / exit remain strict. Added re import. Switch to using cmd_clean (punctuation-stripped) consistently in the strict branches as well.", "files_changed": ["pi_agent.py"], "result": "worked", "better_future_fix": "Eventually have the LLM itself emit a structured mode_switch tool call, so the agent never has to parse natural language for mode intent. Until then, consider an explicit /root and /normie slash-command convention that's both terse and unambiguous.", "lessons": ["Strict command parsing is hostile to natural conversation â€” users phrase the same intent ten different ways", "When the agent fails to recognise an intent, the LLM tries to satisfy the user anyway by miming the missing capability â€” silent failure mode worse than a refusal"], "date": "2026-04-24T22:15:00Z", "recurring": false}
{"id": "S-011", "ticket_ids": ["T-016"], "problem": "_respond_normie() appended only to self.history (a string-only research-mode helper), never to self.messages (the unified Claude-API message store). After mode switched normie -> root, Claude received an empty (or stale) message list. The session looked brand new to Claude, even though the user had had a long conversation seconds earlier. Continuity lost. The L-001 invariant ('mode is not a session boundary') was indirectly violated.", "countermeasure": "(1) _respond_normie now appends both user input and assistant response to self.messages. session_ctx is built from self.messages BEFORE the new user turn is appended, so it is not double-counted. (2) Lifted the safe-truncation logic from _respond_root into a shared _truncate_messages_safely() helper and call it from both modes after each turn.", "files_changed": ["pi_agent.py"], "result": "worked", "better_future_fix": "Eliminate self.history entirely. Have research mode pull its string-only context directly from self.messages via _extract_text_from_messages(). One source of truth, no drift possible.", "lessons": ["Two parallel state stores will always drift â€” drop one or pay the cost forever", "Mode boundaries hide bugs because each mode looks correct in isolation; cross-mode behavior is where invariants break"], "date": "2026-04-24T22:20:00Z", "recurring": false}
{"id": "S-012", "ticket_ids": ["T-020"], "problem": "evolution.py write/read schema drift (SM-001): log_interaction wrote field 'tools_used' (list of name strings), analyze_performance read field 'tool_calls' (list of {id,name,input} dicts). The two field names never agreed, so tool_usage and tool_success_rates returned {} on every call since the analyzer was written. Real production logs (107 interactions across 4 sessions) silently passed through identify_improvements with empty tool data â€” every monthly-self-review tool-failure branch was unreachable.", "countermeasure": "Two edits to evolution.py. (1) log_interaction now writes both 'tools_used' (kept for at-a-glance display) and 'tool_calls' (the structured list, what the analyzer wants). It also extracts session_id from metadata and writes it at the top level of the entry, so per-session log queries do not have to dig into metadata. (2) analyze_performance reads 'tool_calls' first; if absent, materializes [{'name': n} for n in tools_used] from the legacy field â€” every entry already on disk (107 interactions) stays analyzable through the upgrade. Added a per-session breakdown (sessions dict: interactions/successful/failed/cost keyed by session_id) read with the same top-level-first / metadata-fallback pattern.", "files_changed": ["evolution.py", "testing/test_evolution_schema.py"], "result": "worked", "better_future_fix": "The drift was only catchable because someone went looking. A schema test should run as part of run_all_tests.py â€” wired in via Phase 6 verify.py. Also: the entire analyze_performance function does no try/except per-line on json.loads, so one malformed entry crashes the whole report. get_recent_interactions does, inconsistently. Worth unifying.", "lessons": ["Telemetry schemas drift silently because nothing crashes â€” the analyzer just returns empty dicts forever. Test the round trip from log_interaction to analyze_performance with an asserted populated result.", "Backward compatibility on log files matters: a fix that breaks the analyzability of every prior entry is worse than the original drift. Always provide a fallback read path for legacy fields when adding new ones.", "session_id at top level is queryable; session_id buried in metadata is invisible to grep/jq one-liners. If a field is going to be used for grouping, surface it."], "date": "2026-04-25T00:50:00Z", "recurring": false}
{"id": "S-013", "ticket_ids": ["T-017", "T-021"], "problem": "Two related drift bugs in tools/tools_memory.py memory_read. (T-017) Docstring promised tier=None searches all tiers but code at line 96 gated L1 on `if tier == \"l1\":` \u2014 exclusive. Pi reasons about its capabilities from the docstring; the lie produced wrong self-descriptions. (T-021/SM-003) L2 content search filtered on title (first 100 chars of content) only. Distinctive keywords past char 100 were unreachable via memory_read(tier='l2'). Reproduced both: T-017 via testing/test_memory_tier_contract.py; T-021 via testing/test_l2_content_search.py \u2014 wrote an L2 entry with the marker in the body past char 100, searched, got 0 results pre-fix.", "countermeasure": "T-017: rewrote memory_read docstring to match the code \u2014 tier=None searches L3+L2 only, L1 is opt-in via tier='l1' explicitly, with a one-line note about the deferred aggressive fix. Zero code-behaviour change. T-021: in the L2 branch of memory_read, do two queries when query is non-empty \u2014 one ilike on title, one ilike on content->>text (PostgREST JSON-path operator, confirmed working on supabase-py 2.28.3 via direct probe before applying). Merge results by id, dedupe, slice to limit. Empty-query behaviour unchanged.", "files_changed": ["tools/tools_memory.py", "testing/test_memory_tier_contract.py", "testing/test_l2_content_search.py", "testing/test_memory_roundtrip.py", "testing/test_session_id_propagation.py"], "result": "worked", "better_future_fix": "L1 search currently does ilike on raw_wiki.content with no rate limit and no full-text index. The aggressive T-017 fix (include L1 in tier=None) is deferred until raw_wiki has a tsvector index. Adding the index would also speed up L1 explicit searches for users who do pass tier='l1'. L2 content search now does two Supabase round-trips per query; could be folded into one with a PostgREST or_ filter (`title.ilike.*query*,content->>text.ilike.*query*`) once the syntax is locked-down across supabase-py versions.", "lessons": ["A docstring is a contract that something (a future caller, a future reader, or in Pi's case the consciousness prompt) reads and trusts. Lying docstrings cause real misbehaviour even when the code is correct.", "Title-only search with body content under JSONB is the same shape of drift as evolution.tools_used vs tool_calls (S-012/L-011) \u2014 write path stores in one place, read path looks in another. Same class of bug, different layer.", "When a JSON-column filter syntax depends on the exact client-library version, probe it against real data before designing the fix. supabase-py 2.28.3 supports both `.ilike(\"content->>text\", ...)` and `.filter(\"content->>text\", \"ilike\", ...)`; older versions might not."], "date": "2026-04-26T01:00:00Z", "recurring": false}
{"id": "S-014", "ticket_ids": ["T-019"], "problem": "Three prompt-side bugs. (1) C-009: consciousness.txt listed web_search as a tool under 'When to Search vs Remember', in auto-proceed permissions, and in tool-chaining examples â€” the tool does not exist. Every session where Pi tried to satisfy a web-search request was silently lying. (2) C-008: consciousness.txt had no memory_read query-formation rules â€” Claude issued narrative paraphrase queries ('Ash subway order food preferences') instead of short keyword queries ('subway'), producing 0-result searches for content that was clearly in memory. (3) T-019: normie mode refusal was partially covered by the 'Never Mime Tool Use' section but lacked a per-case refusal table, leaving Groq to improvise and sometimes produce persistence claims.", "countermeasure": "Three edits to prompts/consciousness.txt. (1) Replaced the ghost web_search references in 'When to Search vs Remember', auto-proceed, and tool-chain examples with honest 'no web_search tool exists' statements. Removed 'Searching web' from auto-proceed. (2) Added MEMORY_READ QUERY FORMATION RULES section: use 1-3 keywords from user message; first query short and literal; retry with single keyword on 0 results; try tier='l2' after 2 failures; never narrate the search; never invent an answer after 2 failed queries. (3) Added NORMIE MODE REFUSAL TABLE to the Never-Mime-Tool-Use section with exact response phrases for: 'remember X', 'what did I tell you about Y', 'run code', 'search for X', 'switch to root'. Updated tool list to include create_file (was missing) and added 'No web_search, no email, no calendar' footer. Complete rewrite of prompts/system.txt: 19 vague lines â†’ structured normie-context document with identity, mode explanation, full tool suite list (for Groq's awareness), normie refusal table, and hard rules.", "files_changed": ["prompts/consciousness.txt", "prompts/system.txt", "testing/test_normie_honesty.py", "testing/test_query_formulation.py", "testing/test_mode_switch_natural.py"], "result": "worked", "better_future_fix": "test_query_formulation.py's keyword-length assertion (â‰¤4 tokens) may need loosening if Claude legitimately uses 5-token queries that still contain the right keyword. Consider relaxing to 'must contain a stored keyword' as the primary assertion and 'must be â‰¤6 tokens' as a secondary. test_normie_honesty.py requires a live Groq call â€” if Groq is down or rate-limited, the test fails in CI for reasons unrelated to the prompt. Add a GROQ_AVAILABLE skip guard.", "lessons": ["Ghost tool references in the prompt are worse than silence â€” they actively steer Claude toward a capability that doesn't exist and produce confident-sounding lies rather than honest refusals.", "A tool description that says 'call memory_read when you need memory' but says nothing about how to formulate the query produces long narrative queries that miss stored content. The how matters as much as the when.", "A refusal table with exact phrases is more reliable than a general prohibition â€” LLMs under pressure will find the edge of any abstract rule, but specific forbidden/allowed phrase pairs leave no ambiguity."], "date": "2026-05-02T00:00:00Z", "recurring": false}
{"id": "S-015", "ticket_ids": ["T-022"], "problem": "Multiple Python scripts in the testing/ directory printed Unicode glyphs (✓ and ✗) inline with f-strings. On Windows Python 3.13 with default cp1252 stdout, `python testing/test_requirements.py` raised UnicodeEncodeError: 'charmap' codec can't encode character. The env-var checking logic itself worked; only the printout crashed. Same class of bug latent in test_runner.py, run_all_tests.py, test_modes.py, test_persistence.py, test_integration.py. pi_agent.py also printed box-drawing chars (U+2550) and check marks via agent/health.py, but those run only through the pi_agent.py entry point.", "countermeasure": "Two-track fix per T-022 recommendation. (a) Replaced every ✓ with [OK] and every ✗ with [FAIL] across 6 testing/ scripts: test_modes.py, test_integration.py, test_persistence.py, test_requirements.py, test_runner.py, run_all_tests.py. Verified by grep [✓✗] across testing/ → 0 matches. (b) Confirmed pi_agent.py already had `if hasattr(sys.stdout, 'reconfigure'): sys.stdout.reconfigure(encoding='utf-8', errors='replace')` at lines 10-13 (added in earlier Phase 4-6 work, not in this fix). agent/health.py has no __main__ block, so it inherits pi_agent.py's reconfigure when invoked. agent/prompt.py's box-drawing chars in the mode block are emitted only into the system prompt (string), never to stdout, so they're safe regardless.", "files_changed": ["testing/test_modes.py", "testing/test_integration.py", "testing/test_persistence.py", "testing/test_requirements.py", "testing/test_runner.py", "testing/run_all_tests.py"], "result": "worked", "better_future_fix": "Add a CI step that runs each testing/*.py script under PYTHONIOENCODING=cp1252 to catch this class of bug on first commit instead of months later. Could fold into Phase 6's verify.py: `for f in testing/*.py: PYTHONIOENCODING=cp1252 python -c 'import ast; ast.parse(open(f).read())' or just shell out and assert returncode==0`. Also: the choice of [OK]/[FAIL] is plain ASCII but loses some visual contrast; a follow-up could use `ANSI green/red` color codes via a small print helper, falling back to plain on dumb terminals.", "lessons": ["Pretty-printed Unicode (check marks, box drawing, em-dashes) is a portability liability on Windows. Pick ASCII-safe sigils for diagnostic output unless you control the terminal encoding at the entry point.", "A `sys.stdout.reconfigure(encoding='utf-8')` block at the top of every entry point is the easy escape hatch for the box-drawing aesthetic. Use it intentionally; do not assume it propagates to scripts that get launched independently."], "date": "2026-05-02T00:00:00Z", "recurring": false}
{"id": "S-016", "ticket_ids": ["T-023"], "problem": "The Phase 3 round-trip canary (test_memory_roundtrip.py) returned VERDICT: GREEN but agent #2 made zero tool calls. The marker had been synced from Supabase to SQLite by _sync_l3(), surfaced in get_l3_context(), and Claude read it directly from the system prompt. The memory_read tool path \u2014 the path that breaks in production (LOG1/LOG2) \u2014 was never exercised. The test verified storage and L3 ambient injection; it said nothing about whether Claude formulates good memory_read queries.", "countermeasure": "Created testing/test_memory_tool_path.py with three pytest tests. (1) test_memory_read_tool_fires: writes marker to L2 only (organized_memory, which is never loaded into get_l3_context()), creates fresh agent, monkey-patches _execute_tool to capture memory_read calls, asserts captured_queries is non-empty. (2) test_recall_response_contains_marker: asserts l2_marker.lower() in response.lower(). (3) test_query_contains_useful_keyword: asserts the captured query contains \"zx9\", \"codeword\", or \"secret\" \u2014 catching the paraphrase-query failure mode from LOG1/LOG2. Writing to L2 is the key forcing function: L2 is never auto-loaded into L3 context, so Claude must call memory_read or admit it does not know. Marked @pytest.mark.costly (~/usr/bin/bash.002/run). Companion fix S-013 (T-021) had already fixed the L2 content-search filter to search content->>text, not just title \u2014 so the memory_read call path now actually returns the entry.", "files_changed": ["testing/test_memory_tool_path.py"], "result": "worked", "better_future_fix": "The three tests each create a fresh PiAgent, which means 3 Supabase syncs and 3 Claude API calls per run. Could be refactored to share the l2_marker fixture and reuse a single agent across tests via module-scoped agent fixture, or split into separate costly/free markers. Also: test_query_contains_useful_keyword skips rather than fails when no memory_read fired \u2014 this means test ordering matters. Consider asserting the tool call first in a shared fixture step.", "lessons": ["A round-trip test satisfied by ambient context proves nothing about the tool call path. Force the tool path by writing to a tier that cannot be ambiently loaded (L2 for Pi).", "When the test fixture writes a unique enough marker, a query formulation failure is observable (0 results) without needing to inspect Claude text output directly \u2014 check captured query strings."], "date": "2026-05-03T00:00:00Z", "recurring": false}
{"id": "S-017", "ticket_ids": ["T-024"], "problem": "L1 (raw_wiki) was populated only by explicit Claude tool calls to memory_write(tier=\"l1\"), which are rare and not guaranteed. Per-turn conversation capture never happened automatically. Two latent bugs compounded this: (1) memory_write(tier=\"l1\") derived thread_id directly from session_id (8 hex chars), but raw_wiki.thread_id is UUID NOT NULL \u2014 every explicit L1 write silently failed Supabase validation. (2) Without auto-logging, Phase B autonomy (Pi reading its own interaction archive) had no data to read.", "countermeasure": "Three changes. (1) Added MemoryTools.log_turn() -- batch-inserts user row + tool rows (one per call, in order) + assistant row to raw_wiki in a single Supabase insert. Uses a proper UUID thread_id passed by the caller. Best-effort: exceptions print a non-fatal warning and never propagate. (2) Fixed memory_write(tier='l1') thread derivation: replaced session_id-as-thread_id with uuid5(NAMESPACE_DNS, session_id). uuid5 is deterministic per session_id, gives a valid UUID, and produces the same thread_id as log_turn() for the same session -- so tool-path and auto-log rows share thread_id (T-013). session_id is now also stored in L1 metadata for cross-log correlation. (3) In pi_agent.py __init__: added self.l1_thread_id = str(uuid.uuid5(uuid.NAMESPACE_DNS, self.session_id)) and self.turn_number = 0. In _respond_root: added l1_tool_records list populated inside the tool loop with {name, input, result_summary[:500]}, then log_turn() called after evolution.log_interaction. In _respond_normie: log_turn() called after evolution.log_interaction (no tool calls). Also moved import uuid from inside method bodies to top-level in tools_memory.py.", "files_changed": ["tools/tools_memory.py", "pi_agent.py", "testing/test_l1_autolog.py"], "result": "worked", "better_future_fix": "log_turn() fires synchronously, adding ~1 Supabase round-trip per turn. For very fast sessions this is acceptable; for interactive low-latency use, consider fire-and-forget threading (threading.Thread(target=self.memory.log_turn, ...).start()). Also: raw_wiki.timestamp is the same for all rows in a turn (snapshot of turn start time). Turn ordering within a session is stable via turn_number in metadata, but if two turns fire close together, clock-based ordering of threads could interleave. An explicit sequence field on raw_wiki would be cleaner long-term.", "lessons": ["A UUID column that receives a short hex string fails silently at the Supabase constraint level \u2014 the Python client catches the error, logs it, and returns success=False with no visible crash. Always verify the types of all fields against the Supabase schema before writing.", "When two code paths should produce the same UUID for the same logical key (session), use a deterministic derivation (uuid5) rather than storing the UUID separately in both paths \u2014 divergence is guaranteed if the storage drifts."], "date": "2026-05-03T00:00:00Z", "recurring": false}
{"id": "S-018", "ticket": "T-025", "title": "Memory layer completion: L1 TTL, L3 dedup, L1->L2 distillation, row sequencing", "components": ["tools/tools_memory.py", "memory/__init__.py", "memory/pipeline.py", "agent/session.py", "testing/test_memory_pipeline.py"], "what_changed": ["tools/tools_memory.py: added _is_l3_duplicate() \u2014 prefix-based (first 120 chars, case-insensitive, same category) dedup check against SQLite l3_cache before every L3 write. No Supabase round-trip.", "tools/tools_memory.py: added get_l1_thread(thread_id) \u2014 fetches all raw_wiki rows for a session, sorted by (turn, seq) from metadata so within-turn ordering is stable regardless of Supabase return order.", "tools/tools_memory.py: added prune_l1(days=30) \u2014 deletes raw_wiki entries older than N days. Best-effort; catches all exceptions so a Supabase hiccup never blocks session shutdown.", "tools/tools_memory.py: seq field already added to log_turn() entries in previous session (T-025 part 1).", "memory/__init__.py: new package init, exports distill_session.", "memory/pipeline.py: new module \u2014 distill_session() reads L1 thread via get_l1_thread(), calls Groq (free) with structured extraction prompt, writes importance>=4 facts to L2 via memory_write(tier=l2). Handles JSON parse failures and API errors gracefully. dry_run=True supported for testing.", "agent/session.py: on_exit() now calls distill_session() then prune_l1() after writing the L3 session summary. Both calls are wrapped in try/except so a failure never blocks agent shutdown."], "test": "testing/test_memory_pipeline.py", "test_result": "11 tests passed (offline, no API calls): _format_conversation skips tool rows, _extract_facts handles JSON fences and errors, distill_session writes only importance>=4 facts, dry_run suppresses writes, Groq failure returns zero distilled.", "countermeasure": "All new on_exit() calls are wrapped try/except non-fatal. L3 dedup is SQLite-only (no network). Distillation uses Groq (free tier), never Claude (paid).", "closed": "2026-05-03T19:00:00Z"}
{"id": "S-019", "ticket": "T-026", "title": "Memory layer gaps: L2 dedup, L3 expired pruning, L2->L3 auto-promotion", "components": ["tools/tools_memory.py", "agent/session.py", "testing/test_memory_tools_gaps.py"], "what_changed": ["tools/tools_memory.py: added _is_l2_duplicate(content, category) \u2014 prefix match (120 chars, case-insensitive) against organized_memory titles via Supabase; returns None on error so network hiccup never blocks writes.", "tools/tools_memory.py: memory_write(tier=l2) now calls _is_l2_duplicate() before inserting; returns {duplicate: True} with existing id if matched.", "tools/tools_memory.py: added prune_l3_expired() \u2014 deletes past-active_until rows from both Supabase l3_active_memory and SQLite l3_cache; SQLite prune continues even if Supabase fails.", "tools/tools_memory.py: added promote_l2_to_l3(importance_threshold=8) \u2014 queries organized_memory for high-importance active entries, filters via _is_l3_duplicate() (SQLite, no network), writes survivors to L3 via memory_write(tier=l3).", "agent/session.py on_exit(): added promote_l2_to_l3() call after distillation, then prune_l3_expired() call; both wrapped try/except non-fatal.", "testing/test_memory_tools_gaps.py: 12 offline unit tests covering all new code paths."], "test": "testing/test_memory_tools_gaps.py", "test_result": "12 tests passed: _is_l2_duplicate happy/error/empty paths, memory_write L2 dedup skip and write paths, prune_l3_expired Supabase+SQLite and Supabase-error-SQLite-continues, promote_l2_to_l3 writes/skips-duplicate/error/below-threshold.", "countermeasure": "All on_exit() additions are wrapped try/except. _is_l2_duplicate returns None on Supabase error to never block writes. prune_l3_expired tracks Supabase and SQLite results independently.", "closed": "2026-05-03T20:00:00Z"}
{"id": "S-020", "ticket": "build-sprint-1", "title": "Feature sprint: 8 new capabilities added to Pi", "components": ["tools/tools_web.py", "tools/tools_project.py", "tools/tools_obsidian.py", "agent/tools.py", "agent/truncation.py", "pi_agent.py", "testing/test_tools_new.py"], "what_changed": ["tools/tools_web.py: web_search via DuckDuckGo HTML endpoint (no API key, requests only). Returns title/snippet/url for up to 10 results. Full error handling.", "tools/tools_project.py: search_codebase (regex search across Pi source with context lines), create_ticket (auto-ID, writes JSON to tickets/open/), get_session_stats (live turns/cost/tokens/uptime from evolution log).", "tools/tools_obsidian.py: obsidian_read/write/append/search via Obsidian Local REST API (httpx, OBSIDIAN_HOST/OBSIDIAN_API_KEY env vars). Returns structured dicts, graceful unavailable handling.", "agent/tools.py: added all 8 new tool schemas + dispatch cases for web_search, search_codebase, create_ticket, get_session_stats, obsidian_read/write/append/search.", "agent/truncation.py: compress_messages_with_groq() \u2014 at 30+ messages, Groq summarises oldest half into a context block, preserving keep_recent=12 messages verbatim. Free, non-fatal.", "pi_agent.py: _prefetch_memory() \u2014 extracts keyword from user message, runs memory_read, injects PREFETCH block into system prompt before every root turn so Pi always has relevant context.", "pi_agent.py: _truncate_messages_safely() upgraded \u2014 uses Groq compression at 30+ messages, falls back to hard truncation below threshold.", "pi_agent.py: _daily_briefing() \u2014 prints sessions today, cost today, open ticket count, and top L3 context at every startup.", "app/config.py: added OBSIDIAN_HOST and OBSIDIAN_API_KEY env vars.", "testing/test_tools_new.py: 18 offline unit tests covering all new tools. 18/18 pass."], "test": "testing/test_tools_new.py", "test_result": "18 tests passed. Syntax clean: 50 files. Overall verify: 13 non-costly tests pass (1 pre-existing golden test failure unrelated to this sprint).", "closed": "2026-05-03T21:00:00Z"}
{"id": "S-021", "ticket_ids": ["T-024-plan"], "problem": "Normie mode misfired on bare greetings. A plain \"hey\" triggered the memory refusal path, producing responses like \"I am in normie mode, which means I have no tool access\" instead of a normal casual reply. Root cause: the normie mode_block in agent/prompt.py opened with \"YOU ARE NOT IN ROOT MODE. YOU HAVE ZERO TOOLS. THIS IS NOT NEGOTIABLE.\" and an ABSOLUTE RULES section, causing Groq to default to announcing limitations on every turn regardless of whether the user asked for a tool action.", "countermeasure": "Rewrote the normie mode_block in agent/prompt.py with a routing-first structure: (1) default behaviour is respond normally; (2) refusal table is keyword-gated and only consulted when the user message contains explicit tool-action words (remember/save/run/execute/read file etc.); (3) removed the aggressive NOT NEGOTIABLE and ABSOLUTE RULES framing that made Groq lead with limitations; (4) hard rules (no fake persistence claims, no fake mode banners) kept at the bottom. Also fixed two pre-existing regressions from the awareness waterfall rewrite: get_news failure path was missing articles key, get_stocks _stocks_yahoo calls were uncaught. Added test_normie_no_misfire.py to COSTLY_TESTS in verify.py so verify does not fail when all tests are deselected.", "files_changed": ["agent/prompt.py", "tools/tools_awareness.py", "scripts/verify.py", "testing/test_normie_no_misfire.py"], "result": "worked", "better_future_fix": "Long-term: move mode routing into a dedicated layer so the LLM never sees the capability list at all unless it explicitly asks about it. The mode block should be invisible metadata, not part of the conversational context.", "lessons": ["Prompt framing that leads with what the system cannot do trains the LLM to announce limitations unprompted — route first, refuse second", "A refusal table that lacks a trigger condition becomes the default response", "Exception handling in waterfall fallback chains must wrap each source independently — a single uncaught exception bypasses all remaining fallbacks"], "date": "2026-05-03T00:00:00Z", "recurring": false}
{"id": "S-022", "ticket_ids": ["T-025-plan"], "problem": "Raw Groq provider errors reached the user as response text. A 429 rate-limit produced: [Pi] Groq error: Error code: 429 - {\"error\": {\"message\": \"Rate limit reached...\"}}. Additionally, all Groq errors were logged to evolution.jsonl with success=True, giving no signal that anything went wrong.", "countermeasure": "Replaced bare except Exception handler in pi_agent.py _respond_normie with typed except blocks: RateLimitError -> user-friendly rate-limit message; APIStatusError -> generic retry message; fallback Exception -> generic. Raw detail printed internally only. Evolution log now records success=False and error_type (rate_limit / api_error / unknown) in metadata on any Groq failure. Same fix applied to the parallel dead copy in agent/respond.py.", "files_changed": ["pi_agent.py", "agent/respond.py", "testing/test_provider_error_handling.py"], "result": "worked", "better_future_fix": "The _respond_normie method in pi_agent.py and respond_normie in agent/respond.py are diverged copies of the same code. One of them should be deleted and the other should be the single source of truth. This duplication caused the bug — the fix was applied to agent/respond.py first but the running code was in pi_agent.py.", "lessons": ["Two copies of the same function will diverge — pick one authoritative location and delete the other", "Always grep for the function name before editing to confirm which copy actually runs", "Error handlers that always log success=True are worse than no logging — they create false confidence in a system that is silently failing"], "date": "2026-05-04T00:00:00Z", "recurring": false}
{"id": "S-023", "ticket_ids": ["T-026-plan"], "problem": "L3 active context contained the same fact stored 3-10 times under different trailing markers (marker_abc123, unique7x4b). Root causes: (1) _is_l3_duplicate compared raw content[:120] which included the marker, making each variant look distinct. (2) Profile updates created new rows instead of merging into one structured entry, fragmenting identity facts.", "countermeasure": "(T-026b) Added _MARKER_RE regex and _normalize_for_dedup() static method. Both stored and incoming content are marker-stripped before the 120-char prefix comparison in _is_l3_duplicate, so the same fact with different trailing markers now correctly deduplicates. (T-026c) Added _merge_profile() that checks for an existing profile_structured row in SQLite, merges incoming JSON fields in (incoming wins on conflict), and updates the row in-place. memory_write dispatches to _merge_profile before the normal insert path when category==profile_structured.", "files_changed": ["tools/tools_memory.py", "testing/test_t026_dedup_and_profile.py"], "result": "worked", "better_future_fix": "The marker pattern (marker_[hex]) is an artefact of how test fixtures generate unique content. If real Pi conversations stop appending markers, the regex becomes dead code. Consider whether markers should ever appear in production writes, and if not, strip them at write time rather than at dedup time.", "lessons": ["Dedup based on raw string prefix fails whenever content has per-instance suffixes — always normalise before comparing", "Profile data needs slot semantics (merge), not append semantics (insert) — model this explicitly in the write path rather than relying on downstream dedup"], "date": "2026-05-04T00:00:00Z", "recurring": false}
{"id": "S-024", "ticket_ids": ["T-027-plan"], "problem": "Memory prefetch fired on statements of fact (\"i followed up\", \"im planning\") using filler words as queries. Plural queries (\"deadlines\") missed singular stored content (\"deadline\"). Common nouns (\"location\", \"things\") produced no useful results.", "countermeasure": "(Code) _prefetch_memory: added RECALL_SIGNALS set — only fires when message ends in ? or contains a recall signal word. Expanded stop_words with generic nouns. Added plural→singular normalisation before querying. (Prompt) consciousness.txt query formation block replaced with 3-step decision tree: Step 1 decide if search warranted (statements skip), Step 2 pick keyword with proper-noun preference and filler rejection, Step 3 retry singular on zero results with 2-retry cap.", "files_changed": ["pi_agent.py", "prompts/consciousness.txt", "testing/test_query_formulation_v2.py", "scripts/verify.py"], "result": "worked", "better_future_fix": "The _prefetch_memory keyword extraction is still naive (takes first non-stop keyword). A proper NER pass would correctly identify proper nouns (cities, names) over generic ones (planning, location). Could use spaCy NER or a simple regex for CamelCase/Title-case words.", "lessons": ["A search that fires on statements produces noise not signal — always gate prefetch on question detection", "Plural/singular mismatch is a silent killer for exact-match search — normalise at query time not index time", "Rejecting bad query keywords is as important as selecting good ones"], "date": "2026-05-04T00:00:00Z", "recurring": false}
{"id": "S-025", "ticket_ids": ["T-028"], "problem": "system_introspect tool missing — Pi answered self-awareness questions (ticket counts, solution count, interaction count) from stale prompt-derived values instead of reading live files.", "countermeasure": "Added _system_introspect(agent) to agent/tools.py reading live state: evolution.jsonl line count (total_interactions), tickets/open/ and tickets/closed/ glob counts, SOLUTIONS.jsonl line count + last entry ID, SQLite l3_cache row count, agent.session_id, agent.mode, uptime. Added schema to get_tool_definitions() and dispatch elif branch to execute_tool(). Split evolution.jsonl try/except into two independent blocks (total_interactions + last_7d_successes) so a timezone-naive timestamp in older log entries cannot reset total_interactions to None.", "files_changed": ["agent/tools.py"], "result": "worked", "better_future_fix": "Expose system_introspect output as part of the session system prompt prefix so Pi always has current counts without a tool call.", "lessons": ["A tool that silently fails inside a single try/except can zero out earlier-computed values — isolate independent reads into separate try/except blocks", "Self-awareness answers must come from live reads, never from training-time prompt knowledge"], "date": "2026-05-03T00:00:00Z", "recurring": false}
{"id": "S-026", "ticket_ids": ["T-029-plan"], "problem": "L1->L2 distillation wrote 12 facts per session with 0 skipped — small talk, hypotheticals, and Pi responses scored 4-5 by Groq all passed the threshold. Same facts re-written every session because _is_l2_duplicate was never called.", "countermeasure": "Raised minimum importance threshold from 4 to 6 in distill_session. Added _is_l2_duplicate call before each L2 write; skips on match. Updated _SYSTEM prompt with explicit INCLUDE ONLY (stable identity, explicit preferences, firm deadlines) and SKIP lists (greetings, hypotheticals, tentative plans, restatements, Pi responses). Changed max items from 12 to 8. Added per-reason skip breakdown to the [Distill] log line.", "files_changed": ["memory/pipeline.py", "testing/test_memory_pipeline.py"], "result": "worked", "better_future_fix": "Consider a two-pass approach: Groq extracts candidates, a second lighter pass deduplicates against existing L2 content before writing. Would eliminate the Supabase round-trip per fact by batching the dedup check.", "lessons": ["An importance threshold of 4/10 is effectively no threshold — half the scale passes", "Every pipeline write path needs a dedup gate; adding it later always finds duplicates already in storage"], "date": "2026-05-04T00:00:00Z", "recurring": false}
{"id": "S-027", "ticket": "T-030", "title": "Awareness snapshot shortcut \u2014 bypass LLM for weather/market/news", "date": "2026-05-04", "root_cause": "No fast-path existed to extract specific answers from the in-memory awareness snapshot. Both _respond_normie and _respond_root called their LLM backends unconditionally. Snapshot was also only injected into root mode system prompt.", "fix": "Created agent/awareness_shortcut.py: pure module with keyword signal sets (_WEATHER_SIGNALS, _MARKET_SIGNALS, _NEWS_SIGNALS) and line extractors. try_answer_from_awareness(user_message, snapshot) returns None for non-awareness questions and for unavailable data, so the LLM path is unaffected. Wired into _respond_normie and _respond_root as the first block \u2014 before _get_system_prompt. Both shortcut returns log to evolution with model=shortcut, cost=0.0, shortcircuit=True and archive to L1. _get_system_prompt now shares snapshot with normie mode.", "files_changed": ["agent/awareness_shortcut.py (new)", "pi_agent.py (_get_system_prompt: root-only guard removed; _respond_normie + _respond_root: shortcut block added)", "testing/test_awareness_shortcut.py (new, 7 tests)"], "tests": "testing/test_awareness_shortcut.py \u2014 7/7 pass (offline)", "better_future_fix": "Extend signal sets as new awareness sections are added. Cache expiry: if snapshot is older than N minutes, skip shortcut and let LLM fetch fresh data."}
{"id": "S-028", "ticket": "T-031", "title": "source field on memory_write blocks unconfirmed inferences from L3", "date": "2026-05-04", "root_cause": "memory_write had no source parameter. consciousness.txt gave no stated-vs-inferred guidance. Pi stored inferred facts on one-word confirmations (yup/yes) which are acceptable only when the inference is unambiguous.", "fix": "Added source: str = \"stated\" to MemoryTools.memory_write(). L3 branch rejects source=\"inferred_unconfirmed\" immediately with success=False. Added source enum to tool schema in agent/tools.py so Claude can declare the provenance of each write. Added INFERRED VS STATED FACTS section to consciousness.txt: defines stated vs inferred, when one-word confirmations are sufficient, that inferred_unconfirmed is tool-blocked, and that L1/L2 accept inferred entries.", "files_changed": ["tools/tools_memory.py (source param added; rejection guard at top of l3 branch)", "agent/tools.py (source enum in memory_write schema; passed through in dispatch)", "prompts/consciousness.txt (INFERRED VS STATED FACTS section added)", "testing/test_inferred_facts.py (new, 7 tests)"], "tests": "testing/test_inferred_facts.py \u2014 7/7 pass (offline)", "better_future_fix": "Track source in the stored metadata field so past writes can be audited. Add a one-time migration script to back-fill source=stated on existing L3 entries."}
{"id": "S-029", "ticket": "T-032", "title": "Lazy supabase import reduces startup time by ~34%", "date": "2026-05-04", "root_cause": "tools/tools_memory.py had from supabase import create_client at module top. Supabase transitively imports pyiceberg (slow cold-disk chain). Any import pi_agent paid ~1.2s before any code ran.", "fix": "Moved from supabase import create_client into MemoryTools.__init__. Updated three test helpers to remove the now-dead patch(\"tools.tools_memory.create_client\") context managers (all use __new__ so __init__ never ran anyway). import pi_agent: 1.82s -> 1.21s.", "files_changed": ["tools/tools_memory.py (create_client import moved into __init__)", "testing/test_t026_dedup_and_profile.py (removed patch context manager; removed unused patch import)", "testing/test_memory_tools_gaps.py (removed patch context manager; removed unused patch import)", "testing/test_inferred_facts.py (removed patch context manager; removed unused patch import)", "testing/test_lazy_supabase_import.py (new, 2 subprocess-isolated tests)"], "tests": "testing/test_lazy_supabase_import.py \u2014 2/2 pass (subprocess-isolated)", "better_future_fix": "Add a timeout wrapper around MemoryTools.__init__ so a slow supabase import does not block Pi indefinitely on intermittent cold-disk hangs."}
{"id": "S-030", "ticket": "T-033", "title": "Obsidian vault sync + ObsidianTools live API class", "date": "2026-05-04", "root_cause": "tools/tools_obsidian.py did not exist. agent/tools.py had a forward-declared ObsidianTools import that caused ImportError. No vault directory existed. No session-exit sync step.", "fix": "Created tools/tools_obsidian.py with ObsidianTools (REST API wrapper: read/write/append/search, lazy httpx import, graceful degradation) and sync functions (sync_l3_to_vault from SQLite offline, sync_l2_to_vault from Supabase, render_tickets_to_vault JSON->md, render_status_to_vault file copy). All writes atomic (.tmp->os.replace). Created vault/ tree. Wired sync_vault() into on_exit() as non-fatal step. Gitignored vault/memory/ and vault/notes/per-ticket/.", "files_changed": ["tools/tools_obsidian.py (new: ObsidianTools class + 5 sync functions)", "agent/session.py (sync_vault call added to on_exit)", ".gitignore (vault/memory/, vault/notes/per-ticket/, vault/.obsidian/ added)", "vault/README.md (new)", "vault/memory/L3/, vault/memory/L2/, vault/notes/tickets/, vault/notes/per-ticket/ (dirs created)", "testing/test_obsidian_sync.py (new, 18 tests)"], "tests": "testing/test_obsidian_sync.py -- 18/18 pass (offline)", "better_future_fix": "Step 5 (per-ticket notes from ticket JSON) and Step 6 (VS Code Claude prompt update) not yet done -- next session. Add TTL check: skip L2 Supabase sync if last sync was recent."}
{"id": "S-031", "ticket": "T-034", "title": "get_l3_context() duplicate section headers fixed by display-label merge", "date": "2026-05-04", "root_cause": "Different L3 category names (e.g. permanent_profile and profile) both map to PROFILE in the display layer, producing two separate PROFILE: blocks. The render loop grouped by raw key but labelled by display label, creating collisions.", "fix": "Refactored get_l3_context() to group entries by DISPLAY LABEL (via label_map) instead of raw category key. Added aliases profile->PROFILE, projects->PROJECTS, priorities->PRIORITIES, preferences->PREFERENCES to label_map. Also added within-group content dedup: entries whose words are >=80% a subset of another entry in the same group are skipped.", "files_changed": ["tools/tools_memory.py (get_l3_context label_map expanded, grouping by display label, dedup added)"], "tests": "Manual verification with synthetic L3 data; existing 47 tests pass."}
{"id": "S-032", "ticket": "T-035", "title": "Normie mode description corrected: L3 context IS visible (read-only)", "date": "2026-05-04", "root_cause": "The SPECIAL MODES > Normie Mode section only said No tool support, Groq, simple chat. Groq inferred no memory access at all and told Ash it cannot see L1/L2/L3 memory. Factually wrong: L3 context is injected into the system prompt at startup.", "fix": "Updated Normie Mode section in consciousness.txt to explicitly state: L3 context IS loaded into your system prompt (read-only). Added correct refusal wording for write requests.", "files_changed": ["prompts/consciousness.txt (Normie Mode section rewritten)"], "tests": "No automated test; behavioral fix verified by reading updated prompt."}
{"id": "S-033", "ticket": "T-036", "title": "memory_read query formation rules extended to block nonsense queries", "date": "2026-05-04", "root_cause": "Claude triggered memory_read for lemme, restriction, that \u2014 conversational words that are not stored memory keywords. Step 1 check was too loose, Step 2 reject list was too short.", "fix": "Expanded Step 1 with explicit no-search patterns for conversational meta-questions and opinion requests. Expanded Step 2 hard-reject list with 40+ filler/interrogative/common-verb/pronoun words. Added positive check: would this word plausibly appear in a stored memory entry?", "files_changed": ["prompts/consciousness.txt (memory_read Query Formation Rules Steps 1-2 expanded)"], "tests": "No automated test (behavioral). Rejection patterns are explicit now."}
{"id": "S-034", "ticket": "T-037", "title": "Groq-to-Claude handoff: framed context injection + vault archival", "date": "2026-05-04", "root_cause": "On normie->root mode switch, Claude received the raw message history with no framing. It had no signal that it was taking over from Groq. Groq session was never archived.", "fix": "_build_normie_handoff() extracts last <=12 message pairs, formats as GROQ SESSION HANDOFF block. _archive_normie_session_to_vault() writes the session to vault/notes/sessions/YYYY-MM-DD-HHMMSS-normie.md. Both called from process_input() on normie->root switch. Handoff block injected into first root system_prompt via self._normie_handoff_context flag, cleared after first use.", "files_changed": ["pi_agent.py (_normie_handoff_context state, _build_normie_handoff(), _archive_normie_session_to_vault(), process_input() switch handler, _respond_root() injection)"], "tests": "Verified via syntax check and import test. Integration test requires live session."}
{"id": "S-035", "ticket": "T-038", "title": "L3 write-time word-overlap dedup + conflict detection for conflicting facts", "date": "2026-05-04", "root_cause": "L3 could accumulate contradictory facts (e.g. lives in Lawrenceville AND lives in Multan) because the write path only checked exact prefix duplicates. Conflicting location/preference updates were inserted as new rows rather than replacing old ones.", "fix": "_word_overlap() Jaccard-style metric. _check_l3_overlap_and_conflict() compares new content against all category entries: >=0.80 word overlap -> duplicate skip; 0.45-0.80 for CONFLICT_CATEGORIES (permanent_profile, profile, preferences, current_priority) -> supersedes_ids collected. _expire_l3_entry() sets active_until to now in SQLite+Supabase (soft-delete preserving row). Superseded entries expired after successful insert.", "files_changed": ["tools/tools_memory.py (_CONFLICT_CATEGORIES, _word_overlap, _check_l3_overlap_and_conflict, _expire_l3_entry added; memory_write L3 path wired)"], "tests": "Manual verification: conflicting location detected as supersedes, near-dup as duplicate, unrelated as clean. 47/47 existing tests pass."}
{"id": "S-036", "ticket": "T-039", "title": "Universal turn log via process_input wrapper", "date": "2026-05-09T01:58:05.282946+00:00", "root_cause": "Multiple return paths in process_input each had to log L1 themselves; offline failures lost turns.", "fix": "Renamed process_input -> _process_input_inner; new wrapper always appends to logs/turns.jsonl with try/except. New agent/turn_log.py with append_turn/count_today/recent_turns helpers, all best-effort.", "files_changed": ["agent/turn_log.py", "pi_agent.py"], "tests": "testing/test_turn_log.py (10 tests passing)"}
{"id": "S-037", "ticket": "T-040", "title": "Verified normie L1 logging — no fix needed", "date": "2026-05-09T01:58:05.282946+00:00", "root_cause": "Suspected normie path skipped log_turn but audit showed it was already wired (T-024).", "fix": "No code change. Wrapper from S-036 provides defense-in-depth via universal turns.jsonl.", "files_changed": [], "tests": "testing/test_turn_log.py covers wrapper paths."}
{"id": "S-038", "ticket": "T-041", "title": "Compact 3-line startup banner + lazy awareness", "date": "2026-05-09T01:58:05.282946+00:00", "root_cause": "12-line verbose startup made the project feel disorganised; awareness fetched eagerly even when not needed.", "fix": "agent/startup_banner.py::format_banner produces a 3-line summary. health.py default-silent (verbose=False). awareness_snapshot is now an @property with a cache; --eager-awareness flag restores legacy. --verbose-init flag restores legacy print lines. Added 'help' command.", "files_changed": ["agent/startup_banner.py", "agent/health.py", "pi_agent.py"], "tests": "testing/test_startup_banner.py (9 tests passing)"}
{"id": "S-039", "ticket": "T-042", "title": "PI.md master orchestrator + scripts/refresh_pi.py", "date": "2026-05-09T01:58:05.282946+00:00", "root_cause": "Bootstrap state fragmented across 4-5 files; new sessions had no single source of truth.", "fix": "PI.md (~310 lines, 13 sections) at root. scripts/refresh_pi.py regenerates §4/§7/§8/§9 between BEGIN/END markers, idempotent. CLAUDE.md becomes 5-line pointer. 10 stale Phase-0 .md files archived to docs/_archive/.", "files_changed": ["PI.md", "scripts/refresh_pi.py", "CLAUDE.md", "docs/_archive/*"], "tests": "testing/test_refresh_pi.py (12 tests passing)"}
{"id": "S-040", "ticket": "T-043", "title": "Autonomous sprint runner with cost+risk gates", "date": "2026-05-09T01:58:05.282946+00:00", "root_cause": "No mechanism for Pi to autonomously progress through tickets; all work required Ash to drive.", "fix": "scripts/sprint.py picks ticket by severity, generates plan via Claude, optionally auto-implements via Anthropic tool-use loop (read_file/write_file/edit_file/run_bash). RISK_FLAGGED escalates immediately; only SAFE_COMPONENTS auto-implement; default is plan-only. Cost cap, ticket cap, 15-min timeout per ticket. Verify must PASS with 1 retry. Commits to NEW branch sprint/T-NNN-slug; never pushes.", "files_changed": ["scripts/sprint.py"], "tests": "testing/test_sprint_runner.py (24 tests passing)"}
{"id": "S-041", "ticket": "T-044", "title": "Weekly sprint planner", "date": "2026-05-09T01:58:05.282946+00:00", "root_cause": "PI.md §3 had no writer; weekly goal-setting was ad-hoc.", "fix": "scripts/plan_sprint.py — interactive or --auto. Writes §3 in PI.md (regex between §3 and §4 headers). Vault snapshot to vault/notes/sprints/YYYY-Www.md.", "files_changed": ["scripts/plan_sprint.py"], "tests": "testing/test_plan_sprint_and_retro.py (7 plan_sprint tests passing)"}
{"id": "S-042", "ticket": "T-045", "title": "Weekly retrospective generator", "date": "2026-05-09T01:58:05.282946+00:00", "root_cause": "No automated weekly summary; Ash had to inspect logs manually.", "fix": "scripts/retro.py aggregates closed tickets, solutions, turns, evolution, commits for a given ISO week. Renders to vault/notes/retros/YYYY-Www.md. Optional --notify Telegram.", "files_changed": ["scripts/retro.py"], "tests": "testing/test_plan_sprint_and_retro.py (9 retro tests passing)"}
{"id": "S-043", "ticket": "T-046", "title": "VS Code Foam graph for vault knowledge", "date": "2026-05-09T01:58:05.282946+00:00", "root_cause": "Vault graph view lived in standalone Obsidian — separate window from VS Code where Ash codes.", "fix": ".vscode/extensions.json recommends foam.foam-vscode + python ext + markdown-all-in-one. .vscode/settings.json with Foam includeGlobs covering PI.md/vault/CHECKPOINTS/docs and ignoring docs/_archive/pi_env/data/logs. Documented in docs/vscode-setup.md.", "files_changed": [".vscode/extensions.json", ".vscode/settings.json", "docs/vscode-setup.md"], "tests": "Manual install verification per docs/vscode-setup.md"}
{"id": "S-044", "ticket": "T-047", "title": "Whisper STT via faster-whisper \u2014 listen + transcribe_file tools", "date": "2026-05-09T09:05:44.689171+00:00", "root_cause": "Pi had no speech input path. All input was keyboard-only. Phase 8 requires voice-first interface.", "fix": "tools/tools_stt.py: STTTools with transcribe_file(path) and transcribe_mic(seconds). Lazy model load via _get_model() singleton (faster-whisper, default base.en, auto-promotes to cuda+float16 if available). Seconds clamped [1,120]. listen + transcribe_file wired into agent/tools.py dispatch and get_tool_definitions(). STT_MODEL env var for model size override.", "files_changed": ["tools/tools_stt.py", "agent/tools.py", "requirements.txt", "testing/test_stt.py"], "tests": "10 tests \u2014 transcribe_file happy/not-found/error, transcribe_mic success/clamped/missing-dep, tool definitions, agent dispatch", "better_future_fix": "T-048 wake word will call listen() automatically on hotword detection instead of requiring explicit tool call"}
{"id": "S-045", "ticket": "T-069", "title": "Strip duplicate normie refusal table from consciousness.txt", "date": "2026-05-14T18:35:00+00:00", "root_cause": "consciousness.txt held a NORMIE MODE REFUSAL TABLE injected into both modes' system prompts. In root mode the LLM saw 'TOOLS: ENABLED' (mode header) and 'remember X -> refuse' (consciousness) simultaneously and probabilistically picked the refusal text. This was the source of the subway-order hallucination where Pi denied saving while actually saving.", "fix": "Removed the duplicate refusal table from consciousness.txt section 6. Added a one-line note pointing to _NORMIE_MODE_BLOCK_STATIC in agent/prompt.py as the canonical source. The mode block is only injected for normie, so root no longer sees conflicting instructions.", "files_changed": ["prompts/consciousness.txt"], "tests": "Smoke: build_system_prompt_split('root') REFUSAL TABLE False; 'normie' True. 15/15 agent_golden tests pass.", "also_closes": "T-060"}
{"id": "S-046", "ticket": "T-070", "title": "L3 search stopword guard + BM25 score floor", "date": "2026-05-14T18:35:00+00:00", "root_cause": "memory_read passed any string to _hybrid_search_l3, including single-stopword queries like 'remember' or 'look'. BM25 always returns top-N relative scores; combined with the print line, every turn flooded the conversation with junk recall logs.", "fix": "memory_read: when _tokenize(query) yields zero tokens, skip L3 entirely. Suppress the '[Memory] L3 search ...' print line when query had no signal or zero results. _hybrid_search_l3: enforce BM25_FLOOR = 0.5 raw-score minimum.", "files_changed": ["tools/tools_memory.py"], "tests": "Existing memory tests pass. Manual smoke: memory_read('remember') no longer prints noise; memory_read('subway') still returns the order."}
{"id": "S-047", "ticket": "T-071", "title": "Distillation provider fallback chain Groq -> Claude Haiku -> regex heuristic", "date": "2026-05-14T18:35:00+00:00", "root_cause": "Groq free tier daily TPD = 100k tokens. Heavy chat days exhaust it; distill_session called Groq exclusively, so when 429 hit at exit the entire session's L1->L2 distillation failed and facts were lost.", "fix": "memory/pipeline.py: distill_session accepts anthropic_client and tries 1) Groq llama-3.3-70b, 2) Claude Haiku 4.5 (~$0.001 per session), 3) _extract_facts_heuristic regex extractor for 'remember X' / 'my name is' / 'I prefer' / 'deadline X' patterns. agent/session.py + pi_agent.py pass self.claude through.", "files_changed": ["memory/pipeline.py", "agent/session.py", "pi_agent.py"], "tests": "Heuristic smoke: 'my name is Ash, I live in Atlanta, remember oregano bread' -> 3 facts. 47/47 verify tests pass.", "better_future_fix": "Track distillation source in L2 metadata so heuristic-derived facts (lower confidence) are distinguishable from LLM-derived ones."}
{"id": "S-048", "ticket": "T-072", "title": "Mid-session distillation every N turns", "date": "2026-05-14T18:35:00+00:00", "root_cause": "L1->L2 distillation only ran at clean exit. A crash, rate-limit at exit, or abrupt terminal close meant the entire session was lost to L2.", "fix": "PiAgent._maybe_mid_session_distill() called from process_input. Tracks _last_distilled_turn and triggers every 10 turns in a daemon thread. Filters L1 rows by metadata.turn in (last, current] so each batch processes only new rows; L2 dedup handles rare overlaps.", "files_changed": ["pi_agent.py"], "tests": "verify.py PASS (47 tests)."}
{"id": "S-049", "ticket": "T-073", "title": "L2 two-stage prune (archive stale, then hard-delete)", "date": "2026-05-14T18:35:00+00:00", "root_cause": "L2 organized_memory had no cleanup. L3 had prune_l3_expired, L1 had prune_l1, but L2 grew without bound.", "fix": "MemoryTools.prune_l2_stale(archive_after_days=60, delete_after_days=90, archive_importance_below=6). Stage 1: UPDATE status='archived' for active rows older than 60d with importance < 6. Stage 2: DELETE rows already archived for 90+ days. Both best-effort. Wired into agent/session.py::on_exit.", "files_changed": ["tools/tools_memory.py", "agent/session.py"], "tests": "verify PASS. Default L2 query filters status='active' so archived rows are immediately hidden from recall.", "better_future_fix": "Track access count per L2 row; old but actively-read rows should not archive."}
{"id": "S-050", "ticket": "T-074", "title": "Slim normie consciousness \u2014 89.6% prompt reduction", "date": "2026-05-14T18:35:00+00:00", "root_cause": "Normie loaded full root consciousness (30,819 chars / ~7-8k tokens) every turn. Groq free TPD = 100k exhausted in ~25 real turns. Session 26a28eb2 hit 99432/100k after 228 turns.", "fix": "Created prompts/consciousness_normie.txt (~1700 chars). agent/prompt.py::build_system_prompt_split branches on mode: normie uses slim file (cached via _load_normie_consciousness), root uses full. Measured: root 30819 chars vs normie 3214 chars.", "files_changed": ["agent/prompt.py", "prompts/consciousness_normie.txt", "testing/test_agent_golden.py"], "tests": "test_get_system_prompt_includes_consciousness_text updated for both modes. 15/15 agent_golden tests pass."}
{"id": "S-051", "ticket": "T-075", "title": "Lazy Supabase client + background health check", "date": "2026-05-14T18:35:00+00:00", "root_cause": "supabase->realtime->websockets dataclass import chain blocks 3-5s on cold Win+Py3.13. PiAgent __init__ created the client AND ran the health check synchronously before the banner. Ash hit Ctrl+C during the silent wait.", "fix": "1) MemoryTools.__init__ stores url/key only; @property supabase lazy-creates client on first access under _supabase_init_lock. Setter preserved for test mocks. 2) pi_agent.py runs run_health_check in a background daemon thread when --verbose-init is not passed; failures still print to stderr.", "files_changed": ["tools/tools_memory.py", "pi_agent.py"], "tests": "verify PASS. Test mocks that pre-assign self.memory.supabase still work via the setter."}
{"id": "S-052", "ticket": "T-076", "title": "Migrate Gemini provider from google.generativeai to google.genai", "date": "2026-05-14T18:35:00+00:00", "root_cause": "google.generativeai is the deprecated SDK; google-genai (1.70.0) is the current one. Every startup printed a FutureWarning.", "fix": "Rewrote core/providers/gemini.py to use `from google import genai; client = genai.Client(api_key=...)` and `client.models.generate_content(model=..., contents=..., config=types.GenerateContentConfig(...))`. Rewrote core/schema_translate.py::anthropic_to_gemini_tools to emit types.Tool/types.FunctionDeclaration. Public surface unchanged.", "files_changed": ["core/providers/gemini.py", "core/schema_translate.py"], "tests": "Import smoke: from core.providers.gemini import GeminiProvider; from core.schema_translate import anthropic_to_gemini_tools \u2014 clean. verify PASS."}
{"id": "S-053", "ticket": "T-077", "title": "Cerebras as normie primary, Groq fallback", "date": "2026-05-14T19:05:00+00:00", "root_cause": "Normie called Groq llama-3.3-70b-versatile directly. Groq free TPD limit (~100k tokens) was exhausted after 228 turns/day. CEREBRAS_API_KEY was already configured (app/config.py) and CerebrasProvider already existed (core/providers/cerebras.py) but normie never routed through it.", "fix": "pi_agent.py __init__ instantiates self.cerebras as OpenAI(api_key=CEREBRAS_API_KEY, base_url='https://api.cerebras.ai/v1') when key is present. _respond_normie tries Cerebras (model llama-3.3-70b, 1M TPD free, 3-5x faster vs Groq, 16-bit native precision) first; falls back to Groq on any failure. used_provider tracked in evolution.log_interaction for telemetry.", "files_changed": ["pi_agent.py"], "tests": "Smoke: pi_agent imports clean. verify.py PASS expected.", "better_future_fix": "Promote to core/llm_router.py with circuit-breaker and TPD-budget-aware routing per the deep-research recommendation. Track per-provider daily tokens to skip near-limit providers automatically."}
{"id": "S-054", "ticket": "T-078", "title": "L3 temporal validity windows \u2014 invalidate-not-delete on supersession", "date": "2026-05-14T19:05:00+00:00", "root_cause": "T-038 supersession path called _expire_l3_entry which set active_until=past, then T-026's prune_l3_expired hard-deleted the row. Result: when a newer contradicting fact arrived, the historical fact was gone. Pi could never answer 'what was the deadline you told me before you updated it' \u2014 the data was destroyed by the conflict resolution pipeline. Inspired by Zep/Graphiti's validity-window pattern (arXiv 2501.13956).", "fix": "1) SQLite migration adds invalid_at TEXT column via PRAGMA-guarded ALTER (idempotent). 2) Supabase stores invalid_at + superseded_by inside metadata JSONB (no schema migration). 3) New MemoryTools._invalidate_l3_entry(entry_id, by_entry_id) sets invalid_at on both stores without touching active_until \u2014 invalidated rows survive prune_l3_expired. 4) _check_l3_overlap_and_conflict now calls _invalidate_l3_entry instead of _expire_l3_entry. 5) _search_l3_cache, _hybrid_search_l3, get_l3_context all gain `AND invalid_at IS NULL` in their WHERE clause. 6) _sync_l3 reads metadata.invalid_at when refreshing SQLite from Supabase.", "files_changed": ["tools/tools_memory.py"], "tests": "Smoke test exercises full path: ALTER adds invalid_at; insert row; _invalidate_l3_entry sets invalid_at; _search_l3_cache returns 0 rows (filtered). All passed. verify.py PASS expected.", "better_future_fix": "Add a memory_read(query, include_invalidated=True) flag for historical/audit queries. Add an L3 invalidation TTL \u2014 after 180 days of being invalidated, hard-delete the row (longer than the L2 prune window because invalidation preserves history specifically)."}
{"id": "S-055", "ticket": "T-079", "title": "Deprecate knowledge_graph layer \u2014 0 invocations in 24 days", "date": "2026-05-14T19:20:00+00:00", "root_cause": "kg_add_triple / kg_query / kg_search shipped speculatively as a GraphRAG-lite layer. Audit of logs/evolution.jsonl (838 records) + logs/turns.jsonl (1996 records) across 2026-04-20 to 2026-05-14 confirmed ZERO invocations. The L1/L2/L3 + BM25 entity matching already covers entity-relation use cases at one-user scale; the KG layer never earned its keep.", "fix": "1) Moved core/knowledge_graph.py -> docs/_archive/core_knowledge_graph.py and scripts/export_kg.py -> docs/_archive/scripts_export_kg.py (per file-touch policy: archive, never delete). 2) Removed _kg lazy import from agent/tools.py. 3) Removed kg_add_triple, kg_query, kg_search tool definitions (~48 lines) from get_tool_definitions(). 4) Removed three dispatch handlers from execute_tool (~25 lines). Tool count went 76 -> 73. Verified pi_agent imports clean and get_tool_definitions() returns 0 kg_* entries.", "files_changed": ["agent/tools.py", "docs/_archive/core_knowledge_graph.py (moved from core/)", "docs/_archive/scripts_export_kg.py (moved from scripts/)"], "tests": "verify.py PASS (141/141 syntax, 47/47 tests, 0 failures). Smoke: get_tool_definitions() -> 73 tools, no kg_* entries.", "not_done": "Supabase knowledge_graph table is NOT dropped \u2014 contains user data, requires explicit Ash approval. If Ash decides to drop it later, the archived export_kg.py can be restored to read out any data first."}
{"id": "S-056", "ticket": "T-081", "title": "Per-fact L2 vault sync with Foam-graph entity auto-linking", "date": "2026-05-14T19:50:00+00:00", "root_cause": "After T-079 deprecated the L4 knowledge graph (0 invocations in 24 days), Ash still wanted visible proof that Pi's memory was alive and growing. The existing sync_l2_to_vault grouped facts by category into one .md per category, giving Foam only 5-10 nodes to graph total. No useful visualization.", "fix": "Added sync_l2_facts_to_vault() to tools/tools_obsidian.py. Layout: vault/notes/memory/index.md (top-level), vault/notes/memory/<category>.md (category indexes), vault/notes/memory/<category>/<slug>.md (one file per fact). Each fact file has YAML frontmatter (id/category/importance/created_at/importance_bar) and a body with auto-linked entities via _linkify_entities() \u2014 Title-Case sequences become [[Entity]] wiki-links, with a stoplist for I/Pi/Ash/etc to avoid noise. Slugify also fixed to convert underscores to hyphens. Hooked into sync_vault() pipeline so it runs at session exit alongside L3/L2/ticket sync.", "files_changed": ["tools/tools_obsidian.py"], "tests": "Smoke test with 3 fake L2 rows: generated index.md + 2 category indexes + 3 per-fact files; entity linkification verified ('Georgia State University', 'Esra Akbas' wrapped; stoplist entries like 'Ash' / 'Pi' / 'Dr' skipped). Slugify regression test confirmed 'permanent_profile' -> 'permanent-profile'. verify.py PASS.", "better_future_fix": "Mid-session vault sync \u2014 currently runs only at session exit. Hook sync_l2_facts_to_vault into _maybe_mid_session_distill (every 10 turns) so the graph updates in near-real-time without exiting. Also: add entity-page generation (vault/notes/memory/entities/<entity>.md) listing all facts that mention each entity, for a richer entity-hub view in Foam."}
{"id": "S-057", "ticket": "T-082", "title": "Full memory audit system \u2014 rules, digest, CLI, banner, Telegram, 24 tests", "date": "2026-05-14T20:45:00+00:00", "root_cause": "L2/L3 had passive pruning (T-073, T-078) but no proactive review surface. Stale facts accumulated invisibly; Ash couldn't skim, approve, or restore. After L4 deprecation he wanted explicit memory hygiene.", "fix": "Built end-to-end audit system: (1) memory/audit_rules.py \u2014 pure detection functions for stale_low_importance, heuristic_unconfirmed, invalidated_aged, lexical_near_dup, importance_eroded. (2) memory/audit.py \u2014 orchestrator with action ladder (flag\u2192archive\u2192hard-delete), defense-in-depth importance guard (>=7 never auto-archives), audit_state.json persistence, banner formatter, should_run_weekly() gating. (3) scripts/pi_audit.py \u2014 CLI with digest/review/keep/delete --confirm/restore/merge/state subcommands, prefix-match id resolution. (4) tools/tools_obsidian.py::render_audit_digest \u2014 markdown digest at vault/notes/memory/audit/YYYY-Www.md with copy/pasteable shell commands per finding. (5) agent/session.py \u2014 weekly audit + Telegram push wired into on_exit. (6) agent/startup_banner.py \u2014 optional 4th banner line surfacing flagged/archived/merge counts; warns when audit stale > 10d. (7) tools/tools_memory.py \u2014 async access tracking via daemon thread (_bump_access spawns _bump_access_sync) on memory_read+L3 hybrid search, populates last_accessed_at/access_count in metadata JSONB. (8) testing/test_audit.py \u2014 24 golden tests including double-defense importance guard (rule filter + orchestrator downgrade) and idempotency. Pre-build audit also fixed: flush_logs() never called on exit (data loss bug), Ctrl+C did no cleanup, distill source never persisted on L2 rows.", "files_changed": ["memory/audit_rules.py", "memory/audit.py", "scripts/pi_audit.py", "tools/tools_obsidian.py", "agent/session.py", "agent/startup_banner.py", "tools/tools_memory.py", "memory/pipeline.py", "pi_agent.py", "testing/test_audit.py"], "tests": "24/24 audit tests pass. verify.py PASS (145 syntax / 48 test files / 0 failures). Live dry-run against production Supabase L2 found 6 near-duplicate pairs and 0 false-positive archives.", "threats_countered": "1) false-positive deletion \u2192 high-importance(>=7) guard at BOTH rule and orchestrator level; 2) notification fatigue \u2192 weekly digest only, banner is one optional line; 3) digest bloat \u2192 auto-prunes via same rules; 4) heuristic wrong \u2192 only surfaces candidates, hard-delete needs 90d grace OR --confirm; 5) silent rot \u2192 audit_state.json + banner staleness warning > 10d; 6) user ignores digest \u2192 default-keep on neglect, never destructive; 7) L2 archived but L3 derived facts linger \u2192 orchestrator invalidates L3 rows with >0.6 word overlap in same category in same pass; 8) read-write race in mid-session distill \u2192 snapshot semantics, _bump_access is fire-and-forget on daemon thread so reads never block; 9) audit log unbounded \u2192 digest files follow normal vault prune rules.", "better_future_fix": "Embedding-based near-dup (waits on T-080, GEMINI text-embedding-004) for semantic duplicate detection. Mid-session lightweight audit pass (currently only weekly heavy). Interactive TUI for review (`pi audit` enters review mode). Per-fact access trend (not just count) so 'last accessed 90d ago, never accessed before that' is distinguishable from 'consistently read for 12 months then went quiet'."}
{"id": "S-058", "ticket": "T-080", "title": "Semantic L2 dedup \u2014 Gemini embeddings + cosine + Haiku tiebreaker", "date": "2026-05-14T21:30:00+00:00", "root_cause": "Lexical-only dedup (T-026 + T-038) missed semantic paraphrases. 'I prefer halal' vs 'I only eat hand-cut slaughtered meat' share zero stopword-stripped tokens \u2014 both ended up as separate L2 rows polluting distillation output and L3 ambient context.", "fix": "New module memory/semantic_dedup.py with three building blocks: (1) get_embedding(text) \u2014 calls gemini-embedding-001 (3072-dim) via the existing google-genai client; lazy-init Gemini client from GEMINI_API_KEY env var; returns None on any failure for graceful degrade. (2) cosine_similarity(a,b) \u2014 pure math, handles dim mismatch / empty / zero-norm safely. (3) find_semantic_duplicate(new_text, candidates) \u2014 embeds new fact, runs cosine against stored embeddings of same-category L2 rows, returns (dup_id, score, reason) on >=0.90 cosine ('cosine_high') OR on 0.75-0.90 borderline with Haiku tiebreaker ('haiku_confirmed'). Wired into tools_memory.memory_write(tier='l2'): semantic dedup runs BEFORE lexical dedup; embedding of each new fact persisted to content.metadata.embedding (JSONB array) so future writes have something to compare against. No Supabase schema migration \u2014 uses existing metadata field.", "files_changed": ["memory/semantic_dedup.py", "tools/tools_memory.py", "testing/test_semantic_dedup.py"], "tests": "16/16 semantic dedup tests pass (cosine math + routing + Haiku tiebreaker + graceful degradation paths). verify.py PASS (147 syntax files, 49 test files, 0 failures). End-to-end live test against Gemini + Supabase: wrote 'ash favorite programming language is python', then attempted paraphrase 'pythons is ashs favored programming language' \u2014 semantic dedup caught it at cosine 0.96, write skipped, dedup_kind='semantic'.", "cost": "~$0.00002 per L2 write (Gemini embedding), well within free tier. Haiku tiebreaker fires only in 0.75-0.90 borderline band, expected ~1/week, ~$0.001 each.", "threats_countered": "1) false-positive drop > false-positive insert \u2014 any failure (no Gemini key, network error, Haiku unavailable) returns None and caller falls back to write; we never silently lose a real fact to a tooling glitch. 2) Old rows pre-T-080 have no stored embedding \u2014 candidates without embedding are silently skipped, not treated as 'never matches'. 3) Embedding storage cost \u2014 3072 floats per row in JSONB is ~30KB; for Pi's ~50-fact scale that's ~1.5MB total, acceptable. 4) Borderline disagreement between cosine and human judgment \u2014 Haiku tiebreaker only fires in the band where cosine alone is uncertain; on 'don't know' answer from Haiku, we write (safer than dropping).", "better_future_fix": "1) Migrate embeddings to a Supabase pgvector column with IVFFlat index when row count > 1k (read latency would matter then). 2) Output-dimensionality config on Gemini embedding to reduce storage from 3072 -> 768 dims (4x smaller, minor quality cost). 3) Local SQLite embedding cache so dedup queries don't always pull from Supabase. 4) Backfill embeddings for pre-T-080 L2 rows via a one-shot script."}
{"id": "S-059", "ticket": "T-082", "title": "God mode collapse \u2014 ModeConfig + unified _respond_via_config (R1)", "date": "2026-05-16T06:48:21+00:00", "root_cause": "agent/god.py was 633 lines of parallel implementation: own tool dispatch (20 hand-written tools in a private _TOOLS dict), own memory layer (GodMemory SQLite with a non-l3_cache schema, no invalid_at column, no T-082 access counters), own ticket system (GodTickets), own LLM call path (_groq_call / _ollama_call, completely bypassing LLMRouter), own tool-call format (regex-parsed ```tool``` JSON fences). Every improvement to the main agent silently did not apply to god mode. Drift was guaranteed by construction (P4 deviation signal 'subsystem gaining a god_* variant of an existing method'). Already happening: god mode could not use Cerebras (S-053), did not benefit from T-078 temporal validity windows, could not see the 53 newer non-god tools.", "fix": "Collapsed god into a ModeConfig instance routed through a unified response path. 10-step migration: (1) ADR-001 at docs/adr/001-god-as-mode-config.md justified the collapse and listed the 4 privacy invariants. (2) ModeConfig dataclass + registry in agent/modes.py (root / normie / god configs with fields: prompt_path, memory_db, memory_namespace, vault_path, tickets_dir, router_tier, supports_tools, tool_allowlist, max_tokens, public_logging). (3) MemoryTools.__init__ accepts db_path and namespace; namespace='god' installs a _NoopSupabase shim so god memory never reaches public Supabase (privacy-by-file-separation, ADR-001 invariant 5). (4) scripts/migrate_god_memory.py: one-shot legacy god_memory \u2192 l3_cache migration, maps updated_at \u2192 created_at (last-touched signal), drops tags + access_count, renames source table to god_memory_v1_backup. Idempotent; safe re-runs. (5) LLMRouter.chat gains tier='private' kwarg restricting rotation to Groq \u2192 Ollama (parity with shipping god.py, ADR-001 revision). New core/providers/ollama.py with the regex tool-fence parser lifted from god.py + _flatten_tool_results so Anthropic tool_result blocks survive Ollama's no-role-tool wire format. (6) pi_agent._respond_via_config: unified shadow method consuming a ModeConfig, routing through LLMRouter + agent.tools.execute_tool + per-namespace MemoryTools. execute_tool gained a memory_override kwarg threaded through 7 sites (memory_read/write/delete, modify_file write, create_file write, daily_briefing, reflect, _system_introspect). (7) process_input dispatches god through _respond_via_config; PI_GOD_LEGACY=1 env-var hatch kept as deprecation-warned no-op fallback. (8) testing/test_god_uses_unified_path.py with 5 acceptance assertions: routes through LLMRouter (tier=private), routes through execute_tool (memory_override=private), invalid_at column on god_memory.db, all 4 private paths covered by .gitignore (transitive parent coverage), _NoopSupabase shim installed. tickets/god/ + vault/.god/ + docs/_archive/_private/ added to .git/info/exclude (local-only; matches commit 41e37f2 pattern of keeping god paths out of the public .gitignore). Other two private paths (data/god_memory.db, prompts/god_consciousness.txt) already covered by parent data/ and prompts/ entries in .gitignore. (9) Removed _respond_god + the _god global + the 'from agent.god import GodMode' block; archived agent/god.py to gitignored docs/_archive/_private/agent_god_v1.py. (10) This entry + ticket close + refresh_pi.py.", "files_changed": ["docs/adr/001-god-as-mode-config.md", "agent/modes.py", "tools/tools_memory.py", "core/llm_router.py", "core/providers/ollama.py", "core/providers/__init__.py", "scripts/migrate_god_memory.py", "pi_agent.py", "agent/tools.py", "testing/test_god_uses_unified_path.py", ".gitignore", "docs/_archive/_private/agent_god_v1.py (moved from agent/god.py)", "tickets/open/T-095-god-cross-device-private-cloud.json (filed for Phase 9)"], "tests": "5/5 new tests in testing/test_god_uses_unified_path.py PASS. verify.py PASS at every commit boundary (steps 2-9): 150 syntax / 50 tests / 0 failures. Smoke tests for ModeConfig registry, MemoryTools private namespace, OllamaProvider tool_result handling, migrate_god_memory across 5 scenarios (real migration / idempotent / collision / missing-db / dry-run), LLMRouter tier filtering \u2014 all clean. No interactive god-mode smoke test was performed (requires manual Ollama daemon + Groq key); the success criterion is met via the unit-level routing assertion in test_god_routes_through_llm_router.", "line_count_delta": "Active code (excluding tests + scripts): agent/god.py -633, pi_agent.py +136, agent/modes.py +116, tools/tools_memory.py +54, core/llm_router.py +32, core/providers/ollama.py +134, agent/tools.py +7 \u2192 net -154 lines. Far short of the ticket's 'approximately 550 lines' estimate. The unified _respond_via_config was more substantial than projected (~150 lines including 3 helpers) because the loop must reproduce root-mode-quality tool dispatch with per-namespace memory, not the bare LLM-call loop god.py shipped with. The structural win is real (no parallel agent fork, all 53 newer tools available via god allowlist) but the headline line-count target was optimistic.", "privacy_invariants": "All four ADR-001 paths confirmed excluded from git (verified via ): data/god_memory.db + prompts/god_consciousness.txt (covered by parent data/ and prompts/ entries in .gitignore), tickets/god/ + vault/.god/ (listed in .git/info/exclude \u2014 local-only, never committed). Archived agent/god.py lives at docs/_archive/_private/agent_god_v1.py \u2014 also in .git/info/exclude. _NoopSupabase shim asserted by test 5; god memory cannot reach public Supabase.", "better_future_fix": "R8/T-089 (ModeConfig dataclass for response paths) is the natural follow-on: fold _respond_root and _respond_normie into _respond_via_config so all three modes share one path. Private memory deserves its own L1/L2 distillation policy \u2014 currently god has no L1 (matches god.py parity) but no L2 either, so god's long-term knowledge depends entirely on explicit memory_write calls. Vault sync for the private namespace was dropped (god.py's GodMemory.sync_to_vault is gone); R8 should add a ModeConfig-aware vault writer. T-095 (cross-device god) flips the private namespace from local SQLite to a separate Supabase project for multi-surface god access \u2014 Phase 9 work, depends on T-082.", "not_done": "1) Auto-vault-sync on god\u2192other mode transition (god_memory.db -> vault/.god/consciousness.md + missions.md) was removed when agent/god.py archived; not reimplemented. Behavior change: vault/.god/ artifacts no longer auto-update, god_memory.db remains the source of truth. 2) Pre-flight 'is backend reachable' check on god-mode activation was removed; first god turn now surfaces brownout error if both Groq and Ollama are down. Matches root/normie failure mode. 3) No data migration was triggered automatically \u2014 operators with an existing data/god_memory.db must run `python scripts/migrate_god_memory.py` manually (one-shot, idempotent). 4) Cross-device god mode (T-095 filed) deferred to Phase 9. 5) Tool allowlist is 20 names matching current god.py surface; expansion 'because tool X is read-only' is rejected by [[feedback-god-allowlist]] memory \u2014 leaky-by-execute_bash means the allowlist is for planner minimalism, not privacy enforcement."}
{"id": "S-060", "ticket": "T-083 (R2.1 only — R2.2/R2.3 deferred)", "title": "Tool dispatch registry pattern shipped — 74 tools migrated, agent/tools.py 1681→235 lines (86% reduction)", "date": "2026-05-17T00:51:58+00:00", "root_cause": "agent/tools.py was 1681 lines: ~1000 lines of `if/elif tool_name == 'X':` ladder + ~900 lines of hardcoded schema literals in get_tool_definitions(). Schema/dispatch drift was guaranteed because the two lived in different functions ~400 lines apart. Adding a tool required edits at 3-4 sites (schema literal, elif branch, lazy-import factory, success-flag pattern). T-082's memory_override retrofit touched 7 sites for the same reason — a single concern leaked across the elif ladder.", "fix": "Adopted the ToolSpec registry pattern per ADR-002. Migration in 4 steps over one session: (1) ADR-002 documenting the contract + alternatives + migration plan. (2) New agent/tool_spec.py with frozen ToolSpec dataclass (name, description, input_schema, handler, success_predicate, aliases). agent/tools.py gains _registry() lazy loader, _TOOL_MODULES tuple, registry-first dispatch in execute_tool with elif fallback for unmigrated tools. tools/tools_memory.py pilots TOOLS export for 3 memory tools (memory_read/write/delete). (3) Migrated remaining 16 tool modules in 5 batches (A: execution + awareness + project, 16 tools; B: obsidian + gmail + calendar + image_gen + daily_briefing, 15 tools; C: web cluster web_search/browse/reddit_*/scholar/discord, 7 tools; D: media + faces, 10 tools; E: telegram + tts + stt + browser_auto + computer_use + watchers, 22 tools). agent/watchers.py joined _TOOL_MODULES so WatcherManager tools register from there. (4) Deleted the 524-line dead elif ladder from execute_tool; deleted the 44-line _LazyTool + module-level lazy proxy block (each tool module now owns its own lazy singleton via _b()/_cu()/_gmail_inst()/etc.). Registry path also normalized to legacy telemetry shape (track_pattern with pattern_name=f'tool_{name}' + duration metadata) so existing evolution.jsonl analytics keep working.", "files_changed": ["docs/adr/002-tool-registry-pattern.md", "agent/tool_spec.py", "agent/tools.py", "agent/watchers.py", "tools/tools_memory.py", "tools/tools_execution.py", "tools/tools_awareness.py", "tools/tools_project.py", "tools/tools_obsidian.py", "tools/tools_gmail.py", "tools/tools_calendar.py", "tools/tools_image.py", "tools/tools_briefing.py", "tools/tools_web.py", "tools/tools_browse.py", "tools/tools_media.py", "tools/tools_telegram.py", "tools/tools_tts.py", "tools/tools_stt.py", "tools/tools_browser_auto.py", "tools/tools_computer_use.py"], "tests": "verify.py PASS at every batch boundary (A, B, C, D, E, step-4 deletion, post-LazyTool-cleanup): 151 syntax / 50 tests / 0 failures. Smoke tests per batch confirmed: tool count holds at 74 throughout (no losses), no duplicate names, registry dispatch routes memory_read end-to-end with memory_override, unknown-tool path returns structured error dict. Tool count breakdown: 73 original + 1 from T-097 (memory_search_semantic, shipped same session as the second new tool added via the new contract — natural validation).", "line_count_delta": "agent/tools.py: 1681 → 235 lines (-1446, -86%). New scaffolding: agent/tool_spec.py +31, ~+450 lines across 17 tool modules for handlers + TOOLS exports. Net codebase change: roughly -1000 lines while gaining the registry contract, success_predicate field, and alias support for future R2.2 mergers.", "better_future_fix": "R2.2 mergers (next step under T-083): analyze_image/images/video/ocr/document_smart → analyze_media; web_browse/reddit_*/discord_read → fetch; watcher_add/list/remove/status → watcher; computer_run_task moves out of the tool registry to scripts/sprint.py (it's a recursive agent loop, not a leaf tool). Aliases field on ToolSpec already supports the one-release-cycle backward compat. Tool count target: 74 → ~42. R2.3 (weekly invocation audit cron) is independent and small (~half-day). T-083 stays open until both ship; this entry covers R2.1 only.", "not_done": "1) R2.2 tool consolidation (4 mergers) — semantic decisions deserving their own design conversation, not bundled with the structural R2.1 work. 2) R2.3 cron for tool-usage audit. 3) Test coverage for the new registry: only smoke-test-level coverage so far; a tests/test_tool_registry.py with fixtures for each ToolSpec invariant (name uniqueness, schema validity, handler signature contract) is worth filing as a follow-up. 4) agent/tools.py is 235 lines, slightly above the ADR's 'target ~150-200' — the overage is _system_introspect (~80 lines) which is its own concern and could move to tools/tools_project.py in a separate small ticket if you want the public surface even leaner."}
{"id": "S-061", "ticket": "T-096", "title": "Vault patterns from breferrari/obsidian-mind survey — 5 templates, north_star, entity hubs", "date": "2026-05-17T01:03:56+00:00", "root_cause": "S-056 (per-fact L2 vault sync) noted 'entity-page generation' as a better_future_fix but didn't ship it. Pi's vault structure was implicit — no templates for new note types (sessions, decisions, tickets, entities), no single goal-anchor doc smaller than PI.md. Survey of breferrari/obsidian-mind surfaced three patterns worth lifting (north star, templates, entity hubs) and excluded the rest (Claude Code hook coupling, performance-review apparatus, ShardMind/QMD tooling) because they assume vault-as-source-of-truth which is architecturally opposite to Pi's database-backed memory.", "fix": "Three additive changes: (1) vault/notes/templates/ created with 5 Pi-adapted templates (Session.md, Decision.md, Ticket.md, Entity.md, North-Star.md) — agnostic scaffolds with placeholder fields, safe to commit publicly. (2) vault/notes/north_star.md placeholder for personal goal anchor — added to .git/info/exclude so personal content stays local. (3) sync_entity_hubs_to_vault() added to tools/tools_obsidian.py (~130 lines): reads L2 organized_memory, extracts Title-Case entity mentions, builds inverted index entity→facts, writes one .md per entity with ≥2 mentions including backlinks to per-fact files and co-occurrence section. Wired into sync_vault() pipeline AFTER sync_l2_facts_to_vault so hub wiki-links resolve to fact slugs. vault/notes/memory/entities/ added to .git/info/exclude (personal content). _ENTITY_HUB_MIN_MENTIONS=2 threshold filters single-mention entities to keep the hub set meaningful.", "files_changed": ["vault/notes/templates/Session.md", "vault/notes/templates/Decision.md", "vault/notes/templates/Ticket.md", "vault/notes/templates/Entity.md", "vault/notes/templates/North-Star.md", "vault/notes/north_star.md", "tools/tools_obsidian.py", ".git/info/exclude"], "tests": "Smoke test with 4 fake L2 rows (2 multi-mention entities + 1 single-mention skipped): generated 2 hub files with correct backlinks + co-occurrence; index.md generated. verify.py PASS (151 syntax / 50 tests / 0 failures). git check-ignore confirmed both personal paths (north_star.md, entities/) excluded.", "better_future_fix": "1) Mid-session hub refresh — currently runs only at session-exit via sync_vault. 2) Entity disambiguation — 'Apple' (company) vs 'apple' (fruit) currently share a hub if both Title-Cased. 3) Hub deletion when entity drops below min_mentions threshold after L2 prune. 4) Hub graph metrics surfaced in audit digest (most-connected entities, isolated entities).", "not_done": "Templates aren't auto-applied — Pi must explicitly read template content when generating new vault notes. A future ticket could add a `vault_from_template(name, fields)` tool that loads and substitutes. The 5 templates are scaffolds, not enforced contracts."}
{"id": "S-062", "ticket": "T-097", "title": "memory_search_semantic tool — Gemini cosine retrieval exposed to the planner", "date": "2026-05-17T01:03:56+00:00", "root_cause": "T-080 shipped a battle-tested embedding engine (Gemini text-embedding-001 + cosine similarity + Haiku tiebreaker, 16/16 tests) but it only fired in the L2 write-path dedup check. The planner had two retrieval paths exposed as tools — obsidian_search (file grep) and memory_read (BM25 + entity hybrid) — both lexical. A query like 'caching decisions' missed an L2 row titled 'Redis Migration ADR' because BM25 + entity matching share zero tokens. Pi had the engine to do semantic recall; it just wasn't a tool.", "fix": "Added MemoryTools.memory_search_semantic(query, limit=5, threshold=0.5) wrapping memory.semantic_dedup.get_embedding + cosine_similarity. Pulls all L2 organized_memory rows where metadata.embedding exists, scores each, returns top N >= threshold sorted by similarity. Graceful degrade returns [] on: no GEMINI_API_KEY, query embedding failure, private namespace (NoopSupabase), or any exception. Registered as ToolSpec in tools/tools_memory.py via the new T-083 R2.1 registry pattern — 1 ToolSpec append, 1 handler function, no edits to agent/tools.py. Added to god mode tool_allowlist in agent/modes.py (read-only, no privacy concern).", "files_changed": ["tools/tools_memory.py", "agent/modes.py"], "tests": "5/5 smoke tests pass: cosine ranking with threshold filter (2 above, 1 skipped for missing embedding), graceful degrade on no embedding, private namespace returns [], tool registered (registry has 4 memory tools), god allowlist includes the new tool. verify.py PASS (151 syntax / 50 tests / 0 failures). Cost per call: ~$0.00002 (Gemini embedding), free tier covered.", "registry_pattern_validation": "T-097 was the first new tool shipped after the T-083 R2.1 registry landed. Adding it required: (1) one new method on MemoryTools, (2) one _handle_memory_search_semantic function, (3) one ToolSpec entry appended to TOOLS in tools_memory.py, (4) one string in god allowlist. Zero edits to agent/tools.py. That's the contract the ADR-002 promised and it held end-to-end on the first real test.", "better_future_fix": "1) Hybrid retrieval — combine BM25 (memory_read) + cosine (memory_search_semantic) scores into a single rank. 2) Embedding cache layer to avoid re-pulling all L2 rows on every query. 3) Per-category semantic search filter (e.g. 'decisions' category only). 4) Threshold tuning per Pi's actual query distribution — 0.5 is a guess; could be raised once we have usage data."}
{"id": "S-063", "ticket": "T-084", "title": "LLMRouter tier matrix + TPD-budget brownout — R3 closed", "date": "2026-05-17T01:30:00+00:00", "root_cause": "Three coupled gaps in the routing layer: (1) S-053 promoted Cerebras as normie primary but the integration was a direct OpenAI client in pi_agent.py:140-150 with hand-rolled Cerebras→Groq failover in _respond_normie:1116-1155 — two failover code paths instead of one, even after T-082 step 5 added CerebrasProvider to LLMRouter. (2) No cost-aware routing: every call site defaulted to Claude (first in the rotation), so distillation + briefing + normie chat hit the most expensive provider when a cheap one would do. (3) TPD-limit hits caused a silent-fail loop: 429 from Groq → 5-min brownout → 5-min cooldown elapses → try Groq again → 429 again, all day until quota reset. ADR-003 documents the design and alternatives considered.", "fix": "Five-piece ship in T-084. (1) Tier matrix in core/llm_router.py: private (groq→ollama, ADR-001), premium (anthropic→gemini), balanced (anthropic→groq→gemini→cerebras→openrouter, current default), cheap (cerebras→groq→gemini→openrouter, no Claude), fast (cerebras→groq). tier='default' aliases to 'balanced' for back-compat. (2) self.cerebras direct client removed from pi_agent.py __init__; _respond_normie collapsed from 50 lines of hand-rolled failover to one router.chat(tier='cheap') call; rate-limit / api-error / unknown user-facing classification preserved via RuntimeError string inspection. (3) memory/pipeline.py::distill_session gained router kwarg (preferred); when provided, uses router.chat(tier='cheap') with a new _extract_facts_router helper; heuristic stays as final fallback. Both call sites (pi_agent._maybe_mid_session_distill, agent/session.py::on_exit) migrated. Legacy groq_client + anthropic_client kwargs preserved as deprecated for back-compat. (4) CostTracker.record() gains tier kwarg; schema migration adds tier column with DEFAULT 'balanced' (idempotent PRAGMA check) + (provider, ts) compound index. New CostTracker.tokens_today(provider) returns SUM(tokens_in+tokens_out) for today UTC. (5) LLMRouter._is_browned_out now checks PROVIDER_DAILY_TOKEN_BUDGET first: groq=100k, cerebras=1M, gemini=1M, openrouter=50k, anthropic+ollama=None. Each tunable via env var (GROQ_DAILY_TOKEN_BUDGET, etc). At >90% utilization, provider is preemptively browned; the call routes to the next tier preference instead of failing.", "files_changed": ["docs/adr/003-router-tier-and-tpd-budget.md", "core/llm_router.py", "core/cost_tracker.py", "memory/pipeline.py", "pi_agent.py", "agent/session.py", "testing/test_provider_error_handling.py", "testing/test_router_tier_and_tpd.py"], "tests": "9/9 new tests in testing/test_router_tier_and_tpd.py PASS (tier matrix orderings, default-aliases-to-balanced, unknown-tier-fallthrough, record stores tier, default tier is balanced, tokens_today aggregates, TPD brownout fires at threshold, TPD brownout routes to next provider, no-budget providers never TPD-brown). testing/test_provider_error_handling.py updated to mock router.chat instead of legacy groq client — 4/4 pass. verify.py PASS — all syntax + non-costly tests green.", "registry_validation": "T-082 step 5 added Cerebras to the router and tier kwarg for private — T-084 expanded that scaffold cleanly. Tier matrix is one Dict[str, tuple] constant; _providers_for_tier reads it. New tier added in future = one dict entry. Preemptive brownout is one new conditional in _is_browned_out. Schema migration is one ALTER TABLE in _init_db.", "better_future_fix": "1) Remove the 'default' alias once all call sites declare explicit tiers (followup small ticket; grep current call sites for tier=). 2) Tier-aware prompt cache (R10/T-091): premium tier should cache aggressively (Claude prompt cache pays back); cheap tier doesn't need cache (responses are fast and not paid). 3) Cost dashboard `summary()` doesn't yet split by tier — adding a `by_tier` breakdown is a 10-line change in cost_tracker.summary() that I deferred to keep this ticket focused. 4) Daily budget defaults should auto-detect paid vs free plans via API call to provider's quota endpoint (Groq supports /v1/usage, Cerebras docs unclear). 5) S-053's better_future_fix ('Promote to core/llm_router.py with circuit-breaker and TPD-budget-aware routing per the deep-research recommendation') is now satisfied — close S-053 forward-reference.", "not_done": "1) The cost dashboard tier-split mentioned above. 2) Auto-detection of paid-tier limits. 3) The 'default' alias is dead weight after migration — left in place for one release cycle to absorb any forgotten call sites; remove in a small followup ticket. 4) Real-world calibration: TPD_UTILIZATION_THRESHOLD=0.9 is a guess; once we have usage data showing how many tokens a typical day burns, we can tune (likely lower it to 0.8 to give more headroom)."}
{"id": "S-064", "ticket": "T-085", "title": "Resumable session exit + 5 ops moved to mid-session/cron — R4 closed", "date": "2026-05-17T02:00:00+00:00", "root_cause": "agent/session.py::on_exit ran 8 ops sequentially, each wrapped in non-fatal try/except. SIGKILL or daemon crash mid-exit silently lost: session summary, vault sync, audit, prune work, L2→L3 promotion. The promotion miss was the worst — a high-importance fact written this session wouldn't reach tomorrow's ambient L3 context. Exit duration 5-15s with vault sync alone 2-4s, weekly audit 10-30s — user perception 'Pi takes forever to quit'.", "fix": "ADR-005 plan: resumable exit state machine + move 5 ops out of exit. Implemented in 9 steps: (1) _ExitState dataclass + _ExitStep + atomic JSON writes to data/session_exit_state.json. Schema documented in agent/session.py module docstring. (2) on_exit refactored to update state per-step (scaffolding-only commit, no behavior change). (3) resume_exit_if_needed() wired into pi_daemon._get_agent BEFORE server.listen so the daemon never accepts a connection while memory is mid-write from a prior crashed session. (4) L2→L3 promotion + vault_sync moved into pi_agent._maybe_mid_session_distill (background thread, runs after every distill batch every ~10 turns). (5) scripts/passive/memory_prune.py — standalone CLI for L3 expired + L2 stale prunes; PiScheduler runs the same logic daily at 03:00 via _memory_prune_job (replacing the old _l3_prune_job). (6) scripts/passive/weekly_memory_audit.py — standalone CLI for weekly audit; PiScheduler runs it weekly Sunday 02:00 via _weekly_audit_job (with Telegram notify wired). (7) EXIT_STEPS trimmed from 9 to 3: flush_logs + session_summary + finalize. (8) _run_exit_steps rewritten as a data-driven dispatch dict (_EXIT_STEP_BODIES). Tolerant of legacy state files from before the trim: unknown step names get marked completed instead of crashing, and underlying work happens via its new mid-session/cron home anyway (each step is idempotent). (9) Tests: 10/10 in testing/test_resumable_exit.py (fresh state lifecycle, status transitions, load_pending semantics, EXIT_STEPS shape, completion-driven, crash-and-resume, legacy file tolerance, on_exit body AST shape ≤10 statements). verify.py PASS at every gate.", "files_changed": ["docs/adr/005-resumable-exit.md", "agent/session.py", "pi_daemon.py", "pi_agent.py", "tools/tools_scheduler.py", "scripts/passive/memory_prune.py", "scripts/passive/weekly_memory_audit.py", "testing/test_resumable_exit.py"], "tests": "10/10 in testing/test_resumable_exit.py. Plus existing test_provider_error_handling + test_router_tier_and_tpd + test_god_uses_unified_path all pass (no regressions). verify.py PASS at scaffolding boundary, mid-session migration boundary, cron-registration boundary, and final trim boundary.", "behavior_changes": "1) Distillation no longer runs at exit — only mid-session every 10 turns. Sessions under 10 turns don't get distilled. Acceptable: short sessions are usually small-talk that doesn't produce durable facts. 2) Promotion + vault sync now run mid-session AND once at exit was removed; the mid-session run within 10 turns of a write is faster than waiting for exit. 3) Prunes run on daily cron (03:00) instead of every exit — predictable schedule, exit-independent. 4) Audit runs on weekly cron (Sunday 02:00) instead of opportunistically at exit when should_run_weekly trips. 5) Old session_exit_state.json files with legacy step names (promote_l2_l3, vault_sync, etc.) are tolerated: resume marks them completed and continues. 6) Exit duration estimate: was 5-15s, now ≤2s (flush 0.5s + summary 1s + finalize 0.1s). Target ≥50% drop is met conservatively.", "better_future_fix": "1) `_maybe_mid_session_distill` now does distill + promote + vault sync — rename to `_maybe_mid_session_pulse` in a small followup. 2) Cron jobs depend on PiScheduler being up. If daemon is off all week, weekly audit doesn't fire until next Sunday after the daemon comes back. For belt-and-suspenders: should_run_weekly() already checks last-run-time, so a missed week surfaces explicitly on next eligible startup. Could also persist the next-scheduled-run to disk and run-now-if-overdue on scheduler start. 3) The 'final-mile vault_sync at exit' question raised in the ADR — left out for now; mid-session sync after every batch should cover. Revisit after a few sessions of evidence. 4) Tests use MagicMock for the agent — an end-to-end test that actually starts the daemon, crashes it mid-exit, and verifies resume would be stronger. Out of scope for unit coverage.", "not_done": "1) Rename _maybe_mid_session_distill to _maybe_mid_session_pulse (small followup). 2) End-to-end crash-and-resume integration test (needs subprocess + signal sending — Windows-specific awkwardness). 3) Audit + prune jobs share PiScheduler — when scheduler is stopped (e.g. running pi.py one-shot client without daemon), neither cron fires. Acceptable since the daemon is up most of the time on Ash's box; standalone scripts/passive/ scripts cover the manual / cron-outside-pi case. 4) `tier='cheap'` (T-084) propagation into distill_session inside the mid-session distill happens automatically because pi_agent passes router=self.router; no additional wiring needed."}
{"id": "S-065", "ticket": "T-088", "title": "Archive SelfModifier — Phase 5 cruft removed (R7)", "date": "2026-05-17T02:30:00+00:00", "root_cause": "evolution.py:294-389 held a SelfModifier class with modify_consciousness (programmatic find-and-replace on prompts/consciousness.txt) and add_tool (writes raw Python to tools/tool_<name>.py with no syntax check, no agent/tools.py registration, no verify gate). Zero callers across the codebase (confirmed by grep). Dated to Phase 5's 'Pi rewrites itself' ambition. The autonomous-improvement goal now lives in scripts/sprint.py with LLM-edit + diff-review + verify gates.", "fix": "Archived the class to docs/_archive/evolution_self_modifier_v1.py with a header comment explaining why it was abandoned (loaded-gun risk: a future Phase 8+ wiring of LLM output into modify_consciousness produces an unbootable daemon by the time the user notices). Replaced the class in evolution.py with a 4-line comment pointing to the archive. EvolutionTracker (the live class) is unchanged.", "files_changed": ["evolution.py", "docs/_archive/evolution_self_modifier_v1.py"], "tests": "verify.py PASS. Smoke: `python -c 'from evolution import EvolutionTracker'` clean. `grep -r SelfModifier e:/pi --include='*.py'` returns only evolution.py (the explanatory comment) and docs/_archive/ (the archived file). Success criteria met: zero callers, EvolutionTracker import unchanged.", "better_future_fix": "If self-modification returns, the design must include: (a) write proposed edits to tools/_proposed/ (gitignored sandbox), (b) require `python scripts/verify.py` PASS against the proposed state, (c) explicit human approval flag, (d) atomic swap-or-revert backed by git. None of that was in v1; the class was a backup-on-write away from being a single LLM hallucination from a restart loop.", "not_done": "scripts/sprint.py is the live autonomous-improvement path now. Its own safety properties (RISK_FLAGGED files, branch-only commits, verify gate) deserve their own audit ticket if Pi ever auto-edits prompts/consciousness.txt — currently sprint.py allows edits but those are RISK_FLAGGED and require human review."}
{"id": "S-066", "ticket": "T-086", "title": "Sprint × god isolation — sprint.py refuses god tickets; AST-checked interactive-only god entry (R5)", "date": "2026-05-17T02:45:00+00:00", "root_cause": "scripts/sprint.py (autonomy loop) and god mode (privacy/uncensored layer) coexisted with no code-enforced barrier. tickets/god/ is gitignored so sprint never globbed it, but the invariant lived in convention not code. A ticket dropped into tickets/open/ mentioning data/god_memory.db could be picked up by sprint, processed unattended, generate uncensored content under the autonomous code path — violating ADR-001's privacy invariants without anyone noticing until the diff review.", "fix": "Three layers of defence: (1) GOD_FORBIDDEN_PATHS constant at top of scripts/sprint.py listing the 6 god paths (tickets/god/, vault/.god/, prompts/god_consciousness.txt, data/god_memory.db, agent/god.py, docs/_archive/_private/agent_god_v1.py — the last two cover the post-T-082 archived state). (2) _ticket_touches_god_paths() scans every text-ish field of a ticket (component, files_affected, current_state, target_state, migration_plan, title, risk_notes) recursively for any literal forbidden-path string. list_open_tickets() filters matches with a `[sprint] excluding ...` log line. (3) Startup fail-fast in main(): if tickets/open/god/ accidentally exists, refuse with rc=3 before any ticket is picked or LLM call is made. PI.md §10 amended with the forbidden list + explicit policy statement.", "files_changed": ["scripts/sprint.py", "testing/test_sprint_isolation.py", "PI.md"], "tests": "6/6 new tests in testing/test_sprint_isolation.py pass: detector unit test for each of the 6 forbidden paths, clean-ticket negative test, nested-list detection (files_affected with a god path buried in a list), list_open_tickets filter test (fake clean + fake god ticket in temp dir; only clean comes back), main() rc=3 refusal when tickets/open/god/ exists, AST scan of pi_agent.py confirming every `self.mode = \"god\"` assignment sits inside the literal interactive 'god mode' command handler. verify.py PASS — 157 syntax / 53 tests / 0 failures.", "better_future_fix": "1) The path-string match is literal — if Pi adds /god_v2/ or similar, the constant needs updating. A glob-based check would be safer but more complex; literal strings are auditable and obvious. 2) The AST scan in test_pi_agent_god_mode_entry_is_interactive_only walks up 15 lines for context — a more rigorous version would build the actual AST parent chain and assert the enclosing If statement's test condition matches the interactive trigger. Acceptable today because the only place self.mode='god' lives is the one handler; bigger codebase would warrant the AST traversal. 3) Add a second AST test that fails if pi_agent.py grows another `self.mode = ...` assignment without an interactive trigger guard.", "not_done": "Manual smoke: running `python scripts/sprint.py --dry-run` interactively to confirm the [sprint] excluding log line fires would be a stronger demonstration. The unit test suite covers all three defence layers but doesn't exercise the full sprint loop end-to-end. Acceptable — sprint is a 700-line script and a true e2e test would need a real ticket, real Claude call, real verify run."}
{"id": "S-067", "ticket_ids": ["T-121"], "problem": "Vision tool (analyze_image / analyze_video) had a hardcoded two-step fallback: prefer Gemini if key present, else Claude. Gemini 429 (quota exhausted) leaked the raw error string into Telegram replies. No circuit breaker meant repeated 429s in a tight loop. Same pattern would have been re-implemented for STT, TTS, etc.", "root_cause": "Provider selection was hardcoded per-tool with try/except fallback. Each new tool would copy the pattern. Circuit-breaker state would have to be reimplemented per call site. The compression-with-Haiku-fallback (T-092) was the first instance of this pattern; the generalisation was overdue.", "countermeasure": "Built agent/provider_router.py with ProviderRouter class: walks provider list in order, parses 429 retryDelay (Google format + Anthropic retry-after header), 3 consecutive failures opens a 5-min circuit. All cooldown opens recorded via track_silent so silent_failure_watcher catches them. tools_media.py adds four strict provider wrappers (gemini_flash, gemini_pro, claude_haiku_vision, claude_sonnet_vision) and dispatches analyze_image and analyze_video through a singleton router.", "files_changed": ["agent/provider_router.py (NEW)", "tools/tools_media.py", "testing/test_provider_router.py (NEW)"], "result": "worked", "better_future_fix": "Provider router pattern should be applied next to: T-124 thinking layer (Groq -> Haiku fallback uses this pattern), T-115 deferred Groq tool_use_failed brownout, future STT/TTS providers. Each is a copy of this T-121 work.", "lessons": ["The third instance of a pattern is when you extract — compression (T-092) was the first, vision (T-121) is the second-and-extract, thinking layer (T-124) will be the third user", "retry_after_s=0 must not fall back to default — use \"is not None\" check, not \"or\" (truthiness bug found in test)", "Provider chains need both explicit RateLimitError class AND implicit detection (message inspection) — different SDKs raise different exception classes for 429", "Circuit breaker state must be per-process in-memory; persistence would create resume-on-restart bugs where a cooldown outlives the actual rate limit"], "date": "2026-05-24T05:50:42.678720+00:00", "recurring": false}
{"id": "S-068", "ticket_ids": ["T-128"], "problem": "Closing a ticket meant manually moving a JSON file. No enforcement of verify.py PASS, no solution-link check, ADRs got forgotten, effort_actual never recorded. The close path was a single point of drift.", "root_cause": "Discipline depended on humans remembering each step. No tooling encoded the gate. Each missed gate compounded: verify skipped under pressure, solutions not logged, ADRs deferred indefinitely.", "countermeasure": "scripts/close_ticket.py is now the sanctioned close path. Four gates: verify.py PASS, SOLUTIONS.jsonl entry, ADR file (when adr_required), no_new_debt soft gate. --no-solution and --skip-verify flags for legitimate exceptions; --force bypasses ALL gates with track_silent record. Prompts for effort_actual_minutes; writes to ticket JSON. Idempotent (already-closed exits 0).", "files_changed": ["scripts/close_ticket.py (NEW)", "testing/test_close_ticket.py (NEW)"], "result": "worked", "better_future_fix": "Add a pre-commit hook that warns on manual ticket moves to tickets/closed/ — currently the only enforcement is convention. Also wire retro.py to compute estimate-vs-actual bias once 10+ tickets have effort_actual recorded.", "lessons": ["Workflow discipline that depends on memory drifts; encode it as a script", "--force flag with mandatory logging is the right escape hatch — gives flexibility without losing audit trail", "Dogfooding the tool to close its own ticket is the cleanest validation"], "date": "2026-05-24T05:53:07.672170+00:00", "recurring": false}
{"id": "S-069", "ticket_ids": ["T-122"], "problem": "Telegram bot dispatched every message to process_input independently. User sending Hey/Supppp/Hey in 5 seconds got three separate replies. L2 distillation ran on N-message thresholds, fragmenting natural conversational units. There was no notion of a conversational bubble.", "root_cause": "Pi treated messages as atomic. Conversations are not. Humans send incomplete thoughts then finish them; rapid bursts are one intent. Without a bubble abstraction, every downstream concern (recall, distillation, response cost) compounded the fragmentation.", "countermeasure": "agent/bubble.py exports BubbleCollector. Telegram text handler enqueues each message; collector waits PI_BUBBLE_IDLE_MS (default 6000ms) of silence per chat_id, then emits a single Bubble{messages, started_at, ended_at, has_media, reply_targets, closed_reason} to a consumer queue. Bubble force-flushes on (a) idle timeout, (b) max_messages, (c) max_bubble_ms, (d) any media message, (e) lifecycle event via collector.flush(chat_id). TelegramTools._dispatch_bubble runs on the consumer thread, joins text with newlines, calls process_input once, sends typing indicator immediately on bubble-open.", "files_changed": ["agent/bubble.py (NEW)", "tools/tools_telegram.py", "testing/test_bubble.py (NEW)", "testing/test_telegram_bubble_integration.py (NEW)"], "result": "worked", "better_future_fix": "Bubble is currently bound to Telegram only. Voice and future channels should use the same primitive. Also: the consumer thread is single-threaded; if multiple chats produce bubbles simultaneously, they are dispatched serially. For scale this might need a small thread pool, but ordering must be preserved per chat_id.", "lessons": ["A unit of conversation is a primitive that touches every downstream system (recall, distillation, response cost) — defining it first unblocks the rest of Phase 8.8", "Per-chat_id locks plus a shared consumer queue is the right concurrency shape — no global serialisation, no per-chat thread explosion", "Force-flush API is the right escape hatch for lifecycle commands (/exit, /clear) — they need synchronous access to the open bubble", "use_bubble=False default-on is the right migration strategy; tests can opt out without changing production behaviour"], "date": "2026-05-25T02:59:10.417900+00:00", "recurring": false}
{"id": "S-070", "ticket_ids": ["T-127"], "problem": "Fixes and decisions discussed in chat regularly failed to get ticketed. This session alone produced 5+ after-the-fact tickets that lived in conversation for hours before being written down. ticket_candidate_miner (Skill 8) scanned code+logs but not the conversation transcript itself.", "root_cause": "Conversation is a first-class source of engineering signal but the passive skill suite ignored it. The longer a session, the more decisions got lost in chat history that never became tracked work.", "countermeasure": "scripts/passive/conversation_ticket_miner.py (Skill 14) reads last 24h of logs/turns.jsonl via _tail_jsonl, asks Groq llama-3.3-70b to extract fixes/decisions/recurring complaints, dedups against existing ticket titles via cheap word-overlap proxy (threshold 0.7), writes fresh candidates to analysis/conversation_candidates.jsonl. PASS=0, WARN=1-3, FAIL=4+. Wired into passive_daily_digest as Skill 14.", "files_changed": ["scripts/passive/conversation_ticket_miner.py (NEW)", "scripts/passive/passive_daily_digest.py", "testing/test_conversation_ticket_miner.py (NEW)"], "result": "worked", "better_future_fix": "Word-overlap dedup is naive — a real embedding-based dedup (cosine via sentence-transformers, reusing T-080 machinery) would catch rewordings the word-overlap proxy misses. Also: candidates should auto-promote to tickets after N days of repeated extraction without action.", "lessons": ["Passive skills should not just observe code state — conversation IS state", "Cheap proxies (word overlap) are fine for first-pass dedup; embedding cost only justified once false-positive rate hurts", "Strict JSON output from LLM needs code-fence stripping defensively — Groq sometimes wraps despite instructions"], "date": "2026-05-25T03:05:18.628809+00:00", "recurring": false}
{"id": "S-071", "ticket_ids": ["T-125a"], "problem": "Pi L3 was append-only. User is 19 sat there forever, even after the next birthday. User born 2006-08-17 and User is 19 coexisted as independent facts with no derivation link.", "root_cause": "No maintenance loop existed. T-078 added invalid_at as a manual flag; T-080 added embedding dedup as one-off. Neither generalised into a continuous process. Derived facts (age, days-until) need automatic recomputation on a schedule.", "countermeasure": "agent/caretaker.py exports lite(db_path), backfill(db_path), detect_derivable(content), and a formula registry (age_from_birthday, days_until_date, days_since_date). Schema extended (idempotent migration) with kind, source_id, recompute_after, formula columns. memory_write auto-detects derivable content and spawns paired derived rows. Caretaker runs per-bubble (via TelegramTools._dispatch_bubble) and daily (via retention_tick.py). filelock data/caretaker.lock prevents bubble + cron races. ADR-007 documents the lifecycle skeleton (stages B and C in T-125b/T-125c).", "files_changed": ["agent/caretaker.py (NEW)", "docs/adr/007-memory-lifecycle.md (NEW skeleton)", "tools/tools_memory.py (4-column migration + derived-fact spawn on memory_write)", "tools/tools_telegram.py (caretaker.lite invoked per-bubble in _dispatch_bubble)", "scripts/retention_tick.py (caretaker.lite in daily cron)", "testing/test_caretaker_lite.py (NEW)"], "result": "worked", "better_future_fix": "Detection is regex-only (born YYYY-MM-DD). Should extend to: \"my birthday is X\", \"X was born\", \"DOB X\", and ISO-8601 variations. Also: derived rows currently dont have a way to delete themselves when their source is invalidated — needs a cascade in T-125b/c.", "lessons": ["Schema migrations must be idempotent — Pi runs the migration block at every startup", "Soft mutations only (update content, never DELETE) — keeps audit trail and makes rollback trivial", "Filelock is the right primitive when multiple triggers (per-bubble + cron) might race on the same DB", "Test time-jumps with explicit now= injection rather than freezegun — cheaper and clearer"], "date": "2026-05-25T03:22:47.385097+00:00", "recurring": false}
{"id": "S-072", "ticket_ids": ["T-123"], "problem": "When user replied to a previous Pi message in Telegram (quote/tag), Pi could not see the quoted text. It treated the user new reply as standalone. Result: tagged images got re-analysed (wasting vision tokens), tagged text lost its context entirely.", "root_cause": "Telegram exposed reply_to_message on every reply event but Pi handler discarded it. The bubble layer (T-122) started preserving it in reply_targets but nothing acted on those targets yet.", "countermeasure": "memory/recall.py exports recall_referenced(text, db_path, threshold=0.85, limit=3) and format_recall_context(hits). Walks L3 (skipping invalidated and derived-pending rows), embeds each candidate via existing memory.semantic_dedup.get_embedding, cosine-compares against query embedding, returns top-N hits above threshold. TelegramTools._dispatch_bubble now calls recall for each entry in bubble.reply_targets, prepends a RECALLED CONTEXT block to the joined text before dispatch.", "files_changed": ["memory/recall.py (NEW)", "tools/tools_telegram.py (recall prepend in _dispatch_bubble)", "testing/test_recall.py (NEW)"], "result": "worked", "better_future_fix": "For Telegram photo replies, the recall should bypass MediaTools.analyze_image entirely when a hit corresponds to a previous analysis — currently it still re-analyses if a media message arrives. That requires linking media bubbles to their image-analysis L3 entries, deferred to a follow-up ticket.", "lessons": ["Reusing existing infrastructure (memory.semantic_dedup) means new features ship in 2h not 2 days", "Skipping derived-pending placeholders in recall search prevents Pi from quoting (pending recompute from xyz) to itself — a subtle bug avoided by filter in the SQL", "Best-effort failure (recall returns [] on any error) is the right contract for an enhancement layer — never block the main flow"], "date": "2026-05-25T03:31:09.109700+00:00", "recurring": false}
{"id": "S-073", "ticket_ids": ["T-124"], "problem": "Inputs like \"no sauce in subway aghhh\" or \"wdym\" confused Pi. Pi grabbed the closest L3 hit (which might be a noisy session summary) and answered literally. No deliberation step existed between message arrival and main-model dispatch.", "root_cause": "The main response model carried two burdens: understand the user AND craft a response. When input was ambiguous or colloquial, both burdens failed together. Splitting comprehension from generation lets a cheap model handle the first burden well.", "countermeasure": "agent/thinking.py exports normalise(text, history) returning {intent, normalised_query, confidence}. Two-job lite version (recall/clarifier/memory-id deferred to T-129). Provider chain: Groq llama-3.3-70b primary → Claude Haiku 4.5 fallback → silent skip on both-fail. ALWAYS-ON for non-command bubbles. Direct commands (exit/help/mode switches/clear) bypass via _BYPASS_COMMANDS set. TelegramTools._dispatch_bubble runs recall (T-123) and thinking (T-124) in parallel threads after bubble close — combined latency wins over serial.", "files_changed": ["agent/thinking.py (NEW)", "tools/tools_telegram.py (parallel recall+thinking in dispatcher)", "testing/test_thinking.py (NEW)"], "result": "worked", "better_future_fix": "Thinking output is prepended as a context block but the main model may ignore it. Need to A/B test whether reply quality actually improves. Also: thinking should cache results when the same bubble fires multiple times in a row (rare but possible during retries).", "lessons": ["JSON parsing from LLM output needs defensive code-fence stripping AND substring extraction — even with explicit JSON-only instructions, models sometimes wrap", "Bypass set should be explicit and short, not regex — commands are a small enumeration and easier to audit when literal", "Parallel I/O (recall + thinking) is a 2x latency win for almost-free complexity cost — just two threads + join with timeout"], "date": "2026-05-25T03:40:00.351443+00:00", "recurring": false}
{"id": "S-074", "ticket_ids": ["T-126"], "problem": "Telegram had no lifecycle commands. To end a session cleanly, user had to close the chat or wait for natural session timeout. There was no way to reset context without ending the whole bot.", "root_cause": "Sessions in Telegram were ambiguous — when did one end? When did context get cleared? Without explicit commands, the user had no agency over their session lifecycle.", "countermeasure": "/exit and /clear handlers added to tools_telegram.py. Both force-flush the open bubble via BubbleCollector.flush(chat_id) FIRST so the user partial thought is dispatched under the current session_id and not lost. /exit then calls agent.session.on_exit synchronously (runs caretaker + summary). /clear rotates session_id and clears agent.messages + agent.history but leaves memory tier untouched. Help text updated. AFK/back removed per Ash decision.", "files_changed": ["tools/tools_telegram.py", "testing/test_telegram_lifecycle.py (NEW)"], "result": "worked", "better_future_fix": "On /exit, the goodbye message is sent before on_exit completes — Telegram users see goodbye fast. But on_exit can take 5-10s for full memory sync; an interim status message would be friendly.", "lessons": ["Lifecycle commands MUST force-flush the bubble first; otherwise the user last thought is silently discarded", "session_id rotation alone is enough for /clear — touching the memory tier would lose context that should persist", "AFK was removed not because it was hard but because the requirement was unclear; cutting scope is the right call when the design has not landed"], "date": "2026-05-25T03:40:00.351467+00:00", "recurring": false}
{"id": "S-075", "ticket_ids": ["T-125b"], "problem": "T-125a covered derived recompute but near-identical L3 entries (User likes coffee / User loves coffee) still accumulated independently. Search noise grew over time; subway-order-class recall confusion got worse with each session.", "root_cause": "L3 dedup existed as a one-off (T-080) but was never wired as a continuous process. The caretaker pattern from T-125a made the natural home for it.", "countermeasure": "agent/caretaker.full(db) now runs lite() + an embedding dedup pass. Walks active rows (excluding derived/invalidated/already-superseded), groups by category, computes embeddings via memory.semantic_dedup.get_embedding, marks losers with superseded_by = winner_id when cosine >= 0.92. Merge policy: highest importance wins, tied importance breaks by newer created_at, tied created_at breaks by id ordering. Bounded at 500 rows/run. New superseded_by column added to l3_cache (idempotent migration). All L3 search paths (memory_read, _hybrid_search_l3, _l3_fast_path, _search_l3_cache, get_l3_context, memory/recall) now also exclude superseded_by rows. Wired to session-exit (caretaker_full step, 10s budget, daemon thread) and daily cron (retention_tick.py).", "files_changed": ["agent/caretaker.py (full mode + _pick_winner + dedup pass)", "tools/tools_memory.py (superseded_by column migration + 6 query filter updates)", "memory/recall.py (filter superseded_by from candidates)", "agent/session.py (caretaker_full EXIT_STEP)", "scripts/retention_tick.py (full instead of lite in daily cron)", "testing/test_caretaker_full.py (NEW)"], "result": "worked", "better_future_fix": "Cross-category dedup is impossible by design (same-category requirement prevents false merges). But sometimes the SAME fact is written under different categories (note vs profile). Future: cross-category dedup with HIGHER threshold (0.95+) and a category-conflict resolver. Also: when winner is itself later invalidated, losers should NOT be promoted back — they stay superseded.", "lessons": ["Soft mutations (superseded_by, not DELETE) keep an audit trail and let you rollback by clearing the field", "Same-category requirement is the cheapest false-merge defense — works without ML", "Search-path filter updates are easy to miss; grep for the existing filter (invalid_at IS NULL) and add the new one everywhere it appears", "Resumable exit tests should use IN checks, not equality — that future-proofed us against T-125b adding a new step"], "date": "2026-05-26T11:54:24.672735+00:00", "recurring": false}
{"id": "S-076", "ticket_ids": ["T-125c"], "problem": "T-125a + T-125b handled recompute and dedup. Contradicting writes (User lives in Atlanta followed later by User lives in Multan) still coexisted as independent active facts; recall pulled whichever ranked higher, and the latest fact was not authoritative.", "root_cause": "No automatic mechanism existed to detect and invalidate the loser when contradictory values appeared for the same topic. T-078 invalid_at was manual; T-080 dedup was identity-not-contradiction.", "countermeasure": "agent/caretaker.py: added scan_contradictions(db) and deep(db). Contradiction scan groups active rows by (category, topic_key) — topic_key extracted via _topic_key (known relation verbs like lives_in, works_at first; fallback to first-2-content-tokens). When a group has multiple distinct values (via _value_tail), newest wins; older rows get invalid_at set. Deep mode is a Haiku-backed nightly pattern review per category — informational only (logged via track_silent.deep_review). full() now runs lite + dedup + contradiction scan inline. deep() wired into retention_tick.py daily cron. ADR-007 finalised with all four lifecycle states (stated/derived/superseded/invalidated), state transitions, mode table, failure modes + recovery.", "files_changed": ["agent/caretaker.py (+_topic_key, _value_tail, scan_contradictions, _try_haiku_review, deep)", "scripts/retention_tick.py (deep call after full)", "docs/adr/007-memory-lifecycle.md (FINAL)", "testing/test_caretaker_contradictions.py (NEW)"], "result": "worked", "better_future_fix": "Topic-key heuristic uses a fixed list of relation verbs. Misses creative phrasings like \"I am now in X\" without an explicit verb. Future work: cascade invalidation (when source row invalidated, derived children get invalidated too) and embedding-based topic clustering for the long tail.", "lessons": ["Contradiction detection is a topic-clustering problem not a value-equality problem — same value with different topics is fine, same topic with different values is the conflict", "Newest-wins is the right default but can be wrong on typo cases; deep-mode Haiku is the safety net catching those", "Soft invalidation throughout means a wrongly-flagged contradiction is one UPDATE statement away from recovery", "ADR finalisation belongs in the LAST stage of a multi-stage feature — writing it at stage A would have been premature; writing it now captures what actually shipped", "Schema migrations + per-test in-memory schemas need to stay in sync — verify caught a wave of regressions when superseded_by was missing from 4 test files; a shared fixture might prevent this in future"], "date": "2026-05-26T12:19:46.079394+00:00", "recurring": false}
{"id": "S-077", "ticket": "T-138", "title": "Telegram media routes through process_input + plain-text replies (parse_mode=None)", "date": "2026-05-26T13:00:00+00:00", "root_cause": "Two compounding bugs: (1) handle_photo/video/document called MediaTools directly and replied with raw vision text — image never entered process_input, no L1/L2 write, no consciousness applied, no conversational continuity. (2) Bot constructed with parse_mode=Markdown; raw vision output containing unescaped */_ chars caused Telegram 400 \"can't parse entities\" on reply, silently swallowed.", "fix": "Added _send_plain_chunks (parse_mode=None) + _media_route (frames vision/STT output as user_input for process_input). Refactored photo/video/document handlers to: analyze → frame → _process_text → plain reply. Voice handler kept STT+_process_text; only swapped reply path to plain chunks. Vision API failures pass the error string as user_input so Pi acknowledges gracefully instead of going silent.", "files_changed": ["tools/tools_telegram.py", "testing/test_telegram_media_routing.py"], "tests": "6 new tests in test_telegram_media_routing.py + 31 existing telegram/bubble/STT tests still PASS + verify.py PASS (206 syntax-clean, 85 tests, 0 fail)", "better_future_fix": "Wire the _dispatch_bubble media branch (currently stubbed as \"media-only bubble — handler not yet wired\") so rapid media + text in same window get bundled like text bubbles. Separately, add observability events (telegram.photo_received) so future regressions are visible in silent_failures.db."}
{"id": "S-078", "ticket_ids": ["T-130"], "problem": "Per-turn cost and token data (total_cost, t_in, t_out, model, duration_s) computed in _respond_via_config but never surfaced to the user. Budget-steering requires visibility.", "countermeasure": "Added agent/cost_footer.py (pure formatter + env check). format_cost_footer() renders [$cost \u00b7 tok_in \u00b7 tok_out \u00b7 model \u00b7 duration]. emit_if_enabled() prints to stderr (never into final_text) when PI_SHOW_COST=on. Wired into _respond_via_config step 6 inside cfg.public_logging gate \u2014 god mode never emits. Wrapped in try/except per Inv.9 (non-fatal observability).", "files_changed": ["agent/cost_footer.py", "pi_agent.py", "testing/test_cost_footer.py"], "result": "worked", "better_future_fix": "When T-131 status line lands, cost_footer can feed into the status bar instead of a raw stderr line \u2014 single unified UX for all per-turn metadata.", "lessons": ["Observability helpers belong in their own module so they can be unit-tested without instantiating the agent.", "stderr is the correct channel for dev/debug output \u2014 never splice cost info into final_text which all downstream consumers see.", "public_logging gate already encodes the god-mode privacy invariant \u2014 piggyback new observability on it rather than duplicating the check."], "date": "2026-05-27T00:00:00Z", "recurring": false}
{"id": "S-079", "ticket_ids": ["T-131"], "problem": "Pi shows 3-line startup banner then goes silent \u2014 mode, cost, open tickets, L3 depth all invisible after banner scrolls past. Users lose cost awareness mid-session.", "countermeasure": "Added agent/status_line.py: format_status_line() pure formatter (god mode redacts session_id), emit_if_enabled() reads agent.mode/session_id/turn_count/evolution.get_daily_cost/open ticket glob/L3 COUNT(*) and prints to stderr when PI_STATUS_LINE=on. Wired into pi_agent.py run() loop after print(response). Never fires in Telegram/voice paths. Also fixed 2 pre-existing flaky test suites (test_memory.py + test_integration.py): hardcoded unique tokens triggered word-overlap dedup on second run \u2014 replaced with UUID-first content. Fixed UnicodeEncodeError in tools_memory.py print (\u2192 \u2192 ->) and added PYTHONUTF8=1 to verify.py subprocess env.", "files_changed": ["agent/status_line.py", "pi_agent.py", "testing/test_status_line.py", "testing/test_memory.py", "testing/test_integration.py", "tools/tools_memory.py", "scripts/verify.py"], "result": "worked", "better_future_fix": "When T-130 cost footer and T-131 status line both exist, consolidate into a single post-turn metadata line to avoid two separate prints. A unified status bar is cleaner than independent prints.", "lessons": ["L3 dedup strips unique[a-z0-9]{4,} suffixes by design \u2014 test markers must be UUID-first, not UUID-suffix.", "Word-overlap dedup fires on shared stable words even when the marker differs \u2014 6/7 word overlap exceeds the 0.80 threshold.", "Windows cp1252 cannot encode arrow characters in print statements \u2014 always use ASCII -> in log prints or set PYTHONUTF8=1 in subprocess env.", "Observability hooks that read agent state must be module-level-importable for patch() to work in tests \u2014 avoid lazy imports inside functions."], "date": "2026-05-27T00:00:00Z", "recurring": false}
{"id": "S-080", "ticket_ids": ["T-132"], "problem": "Pi startup feels cold \u2014 all session context (last topic, last ticket touched) is available in L3 and fs, but never surfaced at startup.", "countermeasure": "Added _format_continuation_line(mode) to agent/startup_banner.py: reads most-recent category=session_history row from l3_cache via direct sqlite3 (no MemoryTools import to keep startup fast/import-safe), reads last-touched ticket from tickets/closed/ mtime. Formats \"Last session YYYY-MM-DD HH:MM \u00b7 \"summary...\" \u00b7 last touched T-NNN\". God mode queries god_memory.db. All reads wrapped in try/except \u2014 returns empty on any failure. Also updated test_startup_banner.py: changed len(lines) == 3 to >= 3 since continuation line is now optional.", "files_changed": ["agent/startup_banner.py", "testing/test_startup_continuation.py", "testing/test_startup_banner.py"], "result": "worked", "better_future_fix": "Continuation line reads the raw session_history content which is the full session summary (can be verbose). Future: store a dedicated short_summary field alongside the full session_history, or truncate at distillation time.", "lessons": ["Direct sqlite3.connect in startup helpers avoids MemoryTools import chain (~100ms startup overhead) while still reading L3.", "Existing banner tests with exact line-count assertions break when new optional lines are added \u2014 use >= rather than == for line counts.", "god/public db path decision belongs in the formatting helper, passed via mode param already available in format_banner()."], "date": "2026-05-27T00:00:00Z", "recurring": false}
{"id": "S-081", "ticket_ids": ["T-133"], "problem": "L3 memory not user-inspectable — no way to audit or steer Pi memory without raw SQL", "countermeasure": "Created scripts/memory_cli.py standalone argparse script with 4 subcommands: list (SELECT with WHERE filters, JSON output, full_id flag), forget (word-overlap top-3 candidates → confirm → UPDATE invalid_at), pin (UPDATE importance=10 + pinned=1 if column exists), why (full row metadata trace). --god flag requires PI_GOD_CLI=1 env — either alone refuses. test_memory_cli.py: 17 tests using seeded temp SQLite DB.", "files_changed": ["scripts/memory_cli.py", "testing/test_memory_cli.py"], "result": "worked", "better_future_fix": "Add thin shim in pi.py so it can be called as \"pi memory list\" instead of \"python scripts/memory_cli.py list\".", "lessons": ["Standalone scripts with no PiAgent import are the lowest-blast-radius way to expose DB operations", "Both --god flag AND PI_GOD_CLI=1 env required prevents accidental god-mode access from a single typo"], "date": "2026-05-27T00:00:00Z", "recurring": false}
{"id": "S-082", "ticket_ids": ["T-134"], "problem": "Single importance int (1-10) cannot express multi-factor memory salience — a dying grandparent fact and a favorite color fact get the same weight at importance=8", "countermeasure": "memory/salience.py: composite_salience = 0.30*imp_norm + 0.25*surprise + 0.20*goal_alignment + 0.15*recency + 0.10*affect_bonus. All inputs NULL-safe with neutral defaults. 6 new l3_cache columns via PRAGMA idempotent migration. _l3_fast_path + _hybrid_search_l3 BM25 path both updated with 12-col SELECT + OperationalError fallback (NULL aliases for pre-migration schemas). PI_SALIENCE_MODE=composite env gate. Also fixed _tokenize to treat underscores as word separators ([^a-zA-Z0-9\\s]) to fix BM25 matching on UUID-prefixed test content. test_salience.py: 30 tests.", "files_changed": ["memory/salience.py", "tools/tools_memory.py", "testing/test_salience.py"], "result": "worked", "better_future_fix": "Surprise score currently defaults to 0.5 neutral (no real cosine computation at write time). Real novelty detection would require one cosine pass against same-category rows — deferred to T-138.", "lessons": ["Pre-migration schema fallback (NULL aliases via OperationalError catch) is essential for any 12+ column SELECT on a shared test DB", "Tokenize on underscores as well as punctuation — BM25 needs identifier-friendly tokenization", "_tokenize consistency between corpus and query is critical; a corpus token t001_rw_abc123 does not match query token abc123 without underscore splitting"], "date": "2026-05-28T00:00:00Z", "recurring": false}
{"id": "S-083", "ticket_ids": ["T-135"], "problem": "Stale L3 facts never decay — a fact last accessed 6 months ago ranks identically to one accessed yesterday, filling hot context with dead weight", "countermeasure": "effective_importance = importance * exp(-decay_rate * days_since_access). Per-category decay rates (permanent_profile=0.002/d half-life ~350d; session_history=0.05/d half-life ~14d). pinned=1 rows are immune. DecayArchivePolicy in agent/retention.py: rows with effective_importance < 1.0 have active_until set to now (soft-archive, recoverable). Gated by PI_DECAY_ARCHIVE=on env. test_decay.py: 11 tests.", "files_changed": ["memory/salience.py", "tools/tools_memory.py", "agent/retention.py", "testing/test_decay.py"], "result": "worked", "better_future_fix": "Ship PI_DECAY_ARCHIVE behind a 2-week soak flag before enabling archival in prod. Monitor weekly_memory_audit for unexpected mass-archival of useful facts.", "lessons": ["effective_importance computed at query time (never stored) avoids stale snapshots and stays accurate without background jobs", "Archive != delete — active_until=now makes rows invisible to default queries but recoverable via --include-archived; never hard-delete prematurely"], "date": "2026-05-28T00:00:00Z", "recurring": false}
{"id": "S-084", "ticket_ids": ["T-139", "T-140", "T-144", "T-145"], "problem": "memory_delete wiped entire L3 (20 entries) on a single call — _l3_fast_path returned all rows regardless of query match (imp+recency non-zero even with match_score=0), and memory_delete had no bulk guard or true soft-delete", "countermeasure": "(1) _l3_fast_path: added match_score > 0 gate — only content-matching rows returned now. (2) _tokenize: split on underscores ([^a-zA-Z0-9\\s]) so UUID-prefixed content tokenizes correctly for BM25. (3) memory_delete: UUID_PREFIX_RE routes id-looking targets to id LIKE query (not content search). Bulk guard: >3 entries requires force=True. True soft-delete: sets invalid_at (recoverable) instead of hard DELETE. WAL snapshot to data/delete_wal.jsonl before every modification. (4) test_l3_search_fast_path.py: updated assertion to verify non-matching rows are NOT returned (was checking they rank lower). test_memory_delete.py: 11 new tests.", "files_changed": ["tools/tools_memory.py", "testing/test_memory_delete.py", "testing/test_l3_search_fast_path.py"], "result": "worked", "better_future_fix": "memory_delete should ultimately require ID-based deletion only (no content-match deletion) to eliminate false-positive deletion risk entirely. Content-based deletes should go through memory_cli forget which shows candidates and confirms.", "lessons": ["_l3_fast_path non-match filter: importance + recency scores alone should never cause a row to be returned — there must be a content match. This is true for any search-flavored retrieval.", "Soft-delete = invalid_at, never hard DELETE — recoverable is always better for user data", "Bulk guards prevent foot-gun operations: more than N entries requires explicit force flag"], "date": "2026-05-28T00:00:00Z", "recurring": false}
{"id": "S-085", "ticket_ids": ["T-115"], "problem": "Groq 400 tool_use_failed triggered 300s brownout — provider healthy, model just generated a bad tool call. Session blocked from Groq for 5 min after one browse attempt.", "countermeasure": "_is_generation_error(exc) classifier added to LLMRouter. Checks tool_use_failed/context_length_exceeded/content_policy_violation in exception string + status_code==400. Generation errors skip _mark_brownout, log WARNING instead of ERROR. If tools were active when tool_use_failed fires, retries once with tools=[] so model can answer in plain text. Rate-limit 429 and 5xx still brownout as before. 9 new tests.", "files_changed": ["core/llm_router.py", "testing/test_llm_router.py"], "result": "worked", "better_future_fix": "Track generation error rate per provider to detect model degradation without punishing the provider unfairly.", "lessons": ["4xx errors have two different semantics: provider failure (brownout correct) vs model generation failure (brownout wrong). Always classify before penalizing.", "A no-tools retry on tool_use_failed is almost always better than 5 min of silence"], "date": "2026-05-28T00:00:00Z", "recurring": false}
{"id": "S-086", "ticket_ids": ["T-146", "T-147"], "problem": "Awareness shortcut returned Atlanta weather when user asked about Multan; awareness snapshot reproduced verbatim by Groq/Cerebras as response; single-word messages bypassed shortcut incorrectly.", "countermeasure": "_extract_weather now compares user place hints against cached city — returns None on mismatch so LLM uses tools. awareness snapshot header includes read-only instruction (never output verbatim). try_answer_from_awareness requires >= 2 words to fire. LIVE AWARENESS check changed from exact string to substring so header variant still matches.", "files_changed": ["agent/awareness_shortcut.py", "tools/tools_awareness.py"], "result": "worked", "better_future_fix": "Awareness shortcut should cross-reference the snapshot city against all known user locations and refuse to serve cached data for any unfamiliar city.", "lessons": ["Cached data shortcuts must validate that the cache covers the specific request — returning wrong-city weather is worse than returning nothing", "LLMs reproduce system prompt content verbatim when confused; explicit read-only annotations in the prompt help prevent this"], "date": "2026-05-28T00:00:00Z", "recurring": false}
{"id": "S-087", "ticket_ids": ["T-141"], "problem": "Pi claims inability (cant analyze docs) then provides accurate details anyway — erodes trust; also hallucinated image descriptions from context instead of actual vision analysis", "countermeasure": "consciousness_normie.txt: explicit image rules (CAN analyze, describe only what actually seen, never guess from context), document rules (cannot open files, but ACTIVE CONTEXT exception for already-analyzed content), hard T-141 rule (never refuse then answer — pick one), explicit ACTIVE CONTEXT rule (if the fact is there, use it).", "files_changed": ["prompts/consciousness_normie.txt"], "result": "worked", "better_future_fix": "Add a capability pre-check in normie response path that validates: if model is about to refuse AND the answer exists in ACTIVE CONTEXT, skip the refusal.", "lessons": ["Refuse XOR answer — never both. Dual-message patterns (I cant... *then* here is the answer) are worse than either alone.", "Vision hallucination from context is subtle — model latches on to user hints and fabricates plausible descriptions. Explicit prompt instruction to look at the actual image helps."], "date": "2026-05-28T00:00:00Z", "recurring": false}
{"id": "S-088", "ticket_ids": ["T-148", "T-143"], "problem": "Pi could not see its own prior replies. extract_text_from_messages and _build_context only captured blocks via hasattr(block,\"text\") (SDK objects) or tool_result dicts, but _build_assistant_content stores every assistant turn as a {type:text,text:...} dict \u2014 so Pi assistant text was dropped from normie session context, root compression input, AND session-summary L2 distillation. Root cause of T-143 incoherence.", "countermeasure": "Added shared _block_text() helper handling SDK objects + {type:text} dicts + tool_result dicts; routed both extractors through it. Guarded truncate_messages_safely against returning [] (backward-walk fallback then full-list). Added testing/test_context_fidelity.py that stores messages in the REAL production shape (assistant = dict-block list) and asserts replies survive.", "files_changed": ["agent/truncation.py", "testing/test_context_fidelity.py"], "result": "worked", "better_future_fix": "Normie still has only a 5-turn / 300-char string-reconstructed window even with the fix (T-149). Pass a real truncated message array to Groq instead of single_message_ctx, and add the integration coherence harness (T-152) so this class of bug regresses loudly.", "lessons": ["When you change a stored data shape (SDK object -> canonical dict in R8), grep every reader of that data. The extractors were never migrated and the dead .text branch hid it.", "Tests that use synthetic plain-string content cannot catch shape-mismatch bugs. Tests must store data in the exact shape production uses.", "A green test suite measured plumbing, not the emergent property (conversation coherence). Self-reported tickets (T-143) can ship confident-but-wrong root causes \u2014 verify with a repro before trusting the suggested fix."], "date": "2026-05-29T08:30:00Z", "recurring": false}

{
  "id": "T-026",
  "source": "post-T-025 gap analysis",
  "title": "Memory layer gaps: L2 dedup, L3 expired entry pruning, L2->L3 auto-promotion",
  "component": "tools/tools_memory.py, agent/session.py",
  "what_failed": "Three gaps remain after T-025. (1) L2 has no dedup — distill_session() can write the same fact to organized_memory across sessions. (2) Expired L3 entries (active_until < now) are filtered in get_l3_context() queries but never deleted from Supabase or SQLite — they accumulate silently. (3) No L2->L3 promotion pathway — high-importance facts written by distillation to L2 never automatically rise to L3 ambient context.",
  "where_failed": "tools/tools_memory.py: memory_write(tier='l2') has no duplicate check; no prune_l3_expired() method; no promote_l2_to_l3() method. agent/session.py on_exit(): does not call prune or promote for L3.",
  "severity": "P2",
  "status": "closed",
  "created": "2026-05-03T19:00:00Z",
  "closed": "2026-05-03T20:00:00Z",
  "linked_solution": "S-019",
  "fix_summary": "Added _is_l2_duplicate() (Supabase title prefix match, returns None on error), L2 dedup check in memory_write(tier='l2'), prune_l3_expired() (Supabase+SQLite, independent error paths), promote_l2_to_l3(threshold=8) (L2 query -> _is_l3_duplicate filter -> memory_write L3). agent/session.py on_exit() now calls promote then prune for L3 after distillation. All calls non-fatal.",
  "verification": {
    "test": "testing/test_memory_tools_gaps.py",
    "result": "12 tests passed (offline stubs): dedup paths, prune Supabase+SQLite, promotion happy/skip/error/threshold paths"
  }
}
